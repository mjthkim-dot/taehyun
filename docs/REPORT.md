# 실시간 영어 미팅 어시스턴트 — 구현 보고서

> 대상: `meeting-copilot/` · 브랜치 `claude/smoothai-kr-english-rag-n23xqg`
> 설계: [ARCHITECTURE.md](./ARCHITECTURE.md) · 계획·수용 기준: [PLAN.md](./PLAN.md)

## 1. 이 앱이 다른 이유

Smooth AI를 포함한 기존 미팅 코파일럿은 **일반적으로 맞는 영어**를 준다.
이 앱은 **내가 실제로 말할 법한 영어**를 준다. 차이는 한 곳에서 나온다:
발언을 만들기 전에 *내 수업 노트 · 내 과거 미팅 · 내 도메인 용어집*을 검색하고,
거기서 나온 표현을 우선 쓴다. 그리고 **실제로 그 표현을 썼을 때만**
"📚 수업에서 배운 표현" 뱃지를 붙여, 근거 없는 신뢰를 만들지 않는다.

## 2. 파이프라인

```
상대 발화 ── Web Speech(실시간) 또는 탭 오디오→VAD→Whisper
   │
   ├─▶ 자막(interim 점선 / final 확정)
   ├─▶ 한국어 번역 (fast 모델 · 캐시 2레인)
   └─▶ 롤링 요약 배너 (10발화 버퍼 · 3문장 또는 30초 · 단일 비행 + 합류)

[동의 / 반박 / 질문 / 제안] 클릭  또는  한→영 퀵 번역 입력
   │
   1) 질의 구성 — 버튼: 맥락 5문장 + 직전 발화 / 퀵 번역: 입력 문장 단독
   2) 하이브리드 검색 top-3 (BM25 + 코사인 → RRF, 소스 다양성 보정)
   3) 히트 < 2 → RAG 없이 폴백 (지연 최소화)
   4) Claude 생성 — 15단어 이내 · 비즈니스 톤 · 검색 표현 우선
   5) 생성문이 검색 표현의 3어절 이상을 포함 → 📚 뱃지

미팅 종료(⏹)
   ├─▶ 사후 요약 (요점 / 약속 / 후속 액션 / 다음에 쓸 표현)
   ├─▶ 트랜스크립트 자동 색인 (누적 학습)
   └─▶ 복습 자산화 — 표현 10 + 놓쳤을 구간 + 질문 리스트 → SRS 카드(1/3/7일)
```

## 3. 두 가지 안전 기본값

사용자가 명시적으로 요구한 두 제약을 **코드 수준에서** 지킨다.

| 제약 | 구현 | 검증 |
|---|---|---|
| 자동 백그라운드 색인 금지 | 색인 쓰기 경로는 ① `/api/sync`(동기화 버튼) ② 미팅 종료 훅 ③ 사용자가 누른 인제스트 버튼뿐. 기동 시 시드는 `embed=False`로 **네트워크를 타지 않는다**. 스케줄러·폴러 없음 | 호출 지점 감사 + E2E |
| Notion 자동 쓰기 OFF | `/api/notion/save`는 `confirm: true` 없으면 **400**. UI는 페이지 주소 입력 + `confirm()` 확인을 거친다. 토큰이 없으면 "마크다운 복사"로 폴백 | `confirm` 없는 요청 → 400 (실측) |

## 4. 측정 결과

### 4.1 검색 품질 — `tests/rag-eval.ts` (10케이스)

**임베딩 없이(키워드 전용) 10/10 (100%).** 색인 118청크
(용어집 70 · 수업 노트 17 · 트랜스크립트 31).

| 갈래 | 예시 질의 | 기대 | 결과 |
|---|---|---|---|
| 영어 발화 → 용어집 | "your quote came in higher than the other vendor" | total cost of ownership | ✅ |
| 영어 발화 → 용어집 | "worried about being locked in to a single cloud" | vendor lock-in / exit strategy | ✅ |
| 한국어 → 영어 노트 | "다음 미팅이 기대된다고 말하고 싶어요" | looking forward | ✅ |
| 한국어 → 영어 노트 | "커피 주문하는 표현" | Can I get one cappuccino | ✅ |

여기서 두 개의 결함을 찾아 고쳤다.

**(a) 한국어 질의가 영어 노트에 닿지 못했다.**
수업 노트에는 `I am looking forward to {weekend / trip / ...}` 처럼
**한국어가 한 글자도 없는 줄**이 많다. 그런데 하단 퀵 번역은 사용자가
한국어로 친다. 임베딩이 없는 환경에서는 이 두 문자열이 한 토큰도 겹치지 않아
영영 검색되지 않았다. 기존에는 *한국어 노트 → 영문 키워드* 다리만 있었으므로,
**반대 방향 다리**(`_BRIDGE_EN`: 영어 패턴 → 한국어 검색어)를 추가했다.
한국어 질의 5건 중 1건 적중 → 5건 중 5건 적중.

**(b) 퀵 번역 질의가 미팅 맥락에 끌려갔다.**
맥락 5문장을 질의에 붙이면 맥락 쪽 토큰 수가 압도해, 한국어 입력의 의도가
묻히고 검색이 통째로 트랜스크립트로 떨어졌다. `intent == "translate"`일 때는
**입력 문장만** 질의로 쓰고, 맥락은 프롬프트에만 남겨 어조를 잡는 데 쓴다.
근거 3건이 전부 트랜스크립트 → 전부 수업 노트로 바뀌었다.

### 4.2 📚 뱃지 정확도

뱃지는 "검색이 됐다"가 아니라 **"검색된 표현을 실제로 썼다"**일 때만 켜진다.
생성문에 검색 표현의 **연속 3어절**이 나타나야 한다(시제·주어 변화를 허용하기 위해
전체 일치가 아니라 3어절 창을 쓴다). 뱃지에 마우스를 올리면 근거 표현이 보인다.

여기서도 결함이 하나 있었다. 표현 추출기가 *마침표로 끝나는 문장*과 *인용문*만
훑어서, 정작 이름이 "수업에서 배운 표현"인데 **수업 노트에서는 뱃지가 한 번도
켜지지 않았다** (노트는 완결 문장이 아니라 기호·한국어 주석이 섞인 줄이라서).
한국어 사이에 낀 영어 덩어리(3어절 이상)를 함께 뽑도록 고쳐,
`I am looking forward to` 같은 노트 표현으로 뱃지가 정상 동작한다.

### 4.3 E2E — `tests/e2e.mjs` (Playwright, 390×844)

샘플 트랜스크립트 5발화를 주입해 검증했다. **32개 체크 전부 통과, 콘솔·네트워크 오류 0.**

- 자막 5줄 · 상대 발언에만 한국어 번역(내 발언은 번역하지 않아 호출을 아낀다)
- 롤링 요약 배너: "견적가가 경쟁사보다 높다는 반론"
- 4버튼(agree/pushback/ask/propose) 각각: 2안 생성 · **15단어 이내 100%** ·
  근거 표시 · 📚 뱃지 · 카드 표시까지 0.10~0.14초(로컬 모의 공급자 기준)
- 한→영 퀵 번역이 같은 파이프라인을 타고 수업 노트를 근거로 씀
- 제안 카드가 하단 퀵 액션을 가리지 않음
- 수동 동기화 버튼 동작 · 복습 탭 통계 · Notion 저장은 버튼으로만

### 4.4 SRS 간격 (실측)

| 조작 | 단계 | 다음 복습 |
|---|---|---|
| 카드 생성 | 0 | 1.0일 뒤 |
| 맞힘 | 1 | 3.0일 뒤 |
| 맞힘 | 2 | 7.0일 뒤 |
| 틀림 | 0 | 1.0일 뒤 (되돌림) |
| 3회 통과 | 졸업 | 더 이상 뜨지 않음 |

방금 만든 카드는 첫 복습이 **내일**이라 "오늘 볼 카드"가 0이 된다.
그대로 두면 카드가 안 만들어진 것처럼 보이므로, 이 경우 **예정 카드**를
날짜와 함께 보여준다.

### 4.5 Lighthouse

| | 성능 | 접근성 | 권장사항 | SEO |
|---|---|---|---|---|
| 모바일 | 100 | 100 | 100 | 100 |
| 데스크톱 | 100 | 100 | 100 | 100 |

처음에는 접근성 92 / SEO 90이었다. 원인은 `--faint` 색의 명도 대비 3.79:1
(기준 4.5:1)과 meta description 부재. 대비를 올린 건 점수 때문만이 아니다 —
**미팅 중 어두운 화면에서 실제로 잘 안 보이는 톤**이었다.

## 5. 데이터

| 소스 | 규모 | 비고 |
|---|---|---|
| 도메인 용어집 | **70개** (`backend/data/domain-corpus.json`) | 전 항목에 `triggers`(고객이 실제로 하는 말) 포함 |
| 수업 노트 | 17청크 | 진옥 선생님 1~5회차 누적 정리 → [표현 7 / 교정 3 / 예문·설명] 분류 |
| 트랜스크립트 | 31청크 | 미팅 종료 시 누적 |

`triggers`가 왜 필요한가: 고객은 "your quote is higher"라고 말하지
"total cost of ownership"이라고 말하지 않는다. 두 문자열은 **한 단어도 겹치지 않아**
용어집이 영영 검색되지 않는다. 그래서 각 용어에 *고객이 실제로 하는 말*을 함께 심는다.

> 개인 데이터(트랜스크립트·수업 노트)는 로컬 SQLite에만 있고
> `.gitignore`가 `backend/data/store.db*`를 차단한다. 저장소에 올라가는 건
> 도메인 용어집 시드뿐이다.

## 6. 남은 것

| 항목 | 상태 | 메모 |
|---|---|---|
| 임베딩(bge-m3) 활성 | 미적용 (환경에 Ollama 없음) | `ollama pull bge-m3` 후 동기화 버튼 → 의미 검색 활성. 키워드 전용으로도 10/10이라 필수는 아니다 |
| 실모델 지연 | 모의 공급자 기준 측정 | 앞선 실측: Cerebras 0.55s · Groq 0.67s · Gemini 1.34s |
| Notion 실제 쓰기 | 경로·거부 검증 완료, 실제 쓰기는 미수행 | `NOTION_TOKEN`과 페이지 편집 권한이 있어야 하고, 사용자 확인을 거치는 동작이라 임의로 실행하지 않았다 |


---

## 7. 전수 QA와 성능 개선 (2026-08-22)

기능 46건 전수 검사 + 부하·스케일·UI 검사를 돌려 결함을 찾고, **실시간 번역·응답
경로를 최우선**으로 고쳤다. 최종 회귀: 기능 46/46 · 검색 10/10 · E2E 32체크 전부 통과.

### 7.1 핵심 수정 — 동시 검색 지연 100배 폭증

색인 1,721청크에서 검색 단독은 14ms인데 **동시 4건이면 1,268ms, 8건이면 3,327ms**가
됐다. 추측 생성 + 버튼 클릭 + 퀵 번역이 겹치는 건 실사용에서 일상이라, 실시간 제안의
꼬리 지연이 그대로 무너지는 결함이다. 원인은 두 겹:

1. **BM25의 N+1 쿼리** — 매칭 청크마다 `SELECT n_tokens`를 개별 실행. 흔한 단어는
   청크 1,600개가 매칭돼 검색 1회에 소형 쿼리 3,200번이 나갔다. → postings와 chunks를
   JOIN 한 방으로.
2. **GIL 콘보이** — CPU 바운드 점수 계산을 병렬로 돌리면 스레드 경합으로 전체가
   느려진다. → 검색 CPU 구간을 단일 락으로 직렬화 (개별 작업이 10~20ms라 대기가
   거의 없다). 질의 임베딩(네트워크 호출)은 락 밖에서 계산한다.

| 시나리오 (1,721청크) | 수정 전 | 수정 후 |
|---|---|---|
| 검색 동시 4건 p50 | 1,268ms | **60ms** (21×) |
| 검색 동시 8건 p50 | 3,327ms | **105ms** (31×) |
| 미팅 종료 적재 + 검색 4스레드 혼합 | 72.5s | **3.3s** (22×) |

### 7.2 실시간 경로 가드

- **빈 입력이 LLM까지 가던 것 차단** — 번역·제안 모두 400 즉시 반환 (요금·지연 낭비 제거)
- **입력 상한** — 번역 2,000자 절단(자막 목적에 맞게), 제안 said 1,500자·context 3,000자
- **버튼 연타 병합** — 첫 클릭은 즉시(실시간 우선), 300ms 안의 연속 클릭은 마지막
  의도 하나만 LLM으로 (실측: 3연타 → 요청 2건, 카드는 마지막 의도)
- **공급자 복구 즉시화(half-open)** — 장애 후 60초 제외는 다중 공급자 전환용인데,
  키가 하나면 공급자가 살아난 뒤에도 최대 60초 계속 실패했다(실측). 사용 가능한
  공급자가 0이면 제외를 무시하고 다시 찔러본다 → 부활 직후 첫 호출부터 성공 확인

### 7.3 견고성·보안

- GET 핸들러 예외 방어 — `?k=abc`가 int() 예외로 **응답 없이 연결을 끊던 것**을 400으로
- 깨진 JSON·잘못된 카드 id·entries 타입 오류 → 500이 아닌 400 (클라이언트가
  "서버 문제"와 "내 요청 문제"를 구분할 수 있게)
- `k`·`limit` 파라미터 상한 (자원 남용 차단), `/health`도 cross-origin 403 일관 적용
- 외부 유래 텍스트(서버 오류 문자열 · 상대 발화에서 뽑은 용어 후보 · 검색 제목 ·
  공급자 오류 상세)가 innerHTML로 들어가던 5곳에 `esc()` 적용
- LLM 전멸 시 카드에 "⏳ 생성 중…"이 남던 것 → 재시도 안내로 교체
- STT 미설정 안내에 무료 로컬 대안(faster-whisper) 병기

### 7.4 확인만 하고 수정하지 않은 것

- 헤드리스 테스트에서 탭 캡처 시도 후 UI가 먹통되던 현상 — 브라우저 공유 선택
  모달이 클릭을 삼키는 **테스트 환경 아티팩트**로 확정 (fake-UI 플래그로 승인 경로
  검증: 화자 분리 배너까지 정상). 실브라우저 동작과 무관.
- 부하: 동시 8커넥션 전 엔드포인트 오류 0 · 60분 미팅 시나리오 145호출 오류 0 ·
  RSS 57MB — 수정 불요.


---

## 8. 웹 서비스 전환 구현 (2026-08-22)

7장의 QA와 웹 전환 분석에서 나온 블로커를 **1~3단계 전부** 구현했다.
회귀: 기능 46/46 · 웹 계층 16/16 (`tests/web_qa.py`) · 검색 10/10 · E2E 전부 통과 ·
Lighthouse 4개 카테고리 100 유지.

### 8.1 전송 계층 (1단계)

- **HTTP/1.1 keep-alive + 청크 스트리밍** — HTTP/1.0이라 매 호출 TCP 연결을 새로
  맺던 것을 전환. 스트림 응답은 청크 프레이밍으로 감싸 **스트림 직후 같은 연결로
  다음 요청이 이어지는 것을 소켓 레벨에서 검증**했다. 중간 실패는 프레이밍 복구가
  불가능하므로 연결을 끊는다(정상 완료만 종료 프레임).
- **소켓 타임아웃 30초** — slowloris 30연결이 스레드 31개를 무한 점유하던 것이
  31초 후 1개로 회수됨을 실측.
- **본문 상한 8MB** — `Content-Length: 99999999` 선언 → 413 즉시 반환 + 연결 종료
  (읽지 않은 본문이 keep-alive의 다음 요청을 오염시키지 않게).
- 보안 헤더: nosniff · Referrer-Policy · 앱 셸에 CSP(`connect-src 'self'` — XSS가
  생겨도 데이터가 밖으로 못 나가게) · Permissions-Policy.

### 8.2 인증·격리·쿼터 (2단계)

- `backend/auth.py`: scrypt 접속 코드 해시, HMAC 서명 무상태 세션(30일),
  IP별 지수 락아웃(5회 무료 → 2^n초), 타이밍 균일화(없는 계정도 해시 1회).
- **물리적 데이터 격리**: 사용자 1명 = `u/<id>/store.db` 1개. 격리 검증 —
  alice가 넣은 노트가 bob의 검색에 안 나오고, bob의 전체 삭제가 alice에 영향 없음.
- **무인증 공개 차단**: 외부 바인딩 + 사용자 0명 → 기동 거부.
- 사용자별 LLM 쿼터: 분당(메모리 슬라이딩 윈도) + 일일(DB 영속). 초과 시 429와
  한국어 사유. 검증 — RPM=3 설정에서 `[200,200,200,429,429]`.
- `manage.py`: adduser/passwd/disable/deluser(--purge)/adopt(기존 로컬 데이터
  이관 — 파일 복사가 아니라 sqlite backup API)/backup.
- 로그인 UI: 오버레이(브라우저 검증 — 오답 사유 표시·세션 유지·로그아웃),
  API 401은 어디서 나오든 로그인 화면으로 복귀.

### 8.3 운영 (3단계)

- 관측: 엔드포인트별 p50/p95/오류 링버퍼 + 오늘 사용자별 LLM 호출 수 —
  관리자 전용 `GET /api/admin/stats`.
- STT 동시성 세마포어(기본 코어/2) — CPU 바운드 Whisper가 서로를 굶기지 않게.
- SIGTERM 그레이스풀 셧다운(진행 중 응답 완료 후 종료 — 실측).
- 배포 자산: `deploy/Caddyfile`(flush_interval -1 = 스트리밍 버퍼링 방지),
  `deploy/nginx.conf.example`(proxy_buffering off), systemd 유닛(DynamicUser·
  ProtectSystem=strict, 코드 /opt · 데이터 /var/lib 분리), `docs/DEPLOY.md`.
- `MC_DATA_DIR` — 코드(저장소 자산)와 런타임 개인 데이터의 물리 분리.

### 8.4 성능 재확인

전송 계층 전환 후 동시 8커넥션: translate p50 19ms / suggest p50 96ms, 오류 0
(모의 공급자 기준). 검색 직렬화·N+1 제거 효과는 7장 그대로 유지.

### 8.5 프레임워크 이관을 하지 않은 이유 (기록)

실측 병목은 LLM 쿼터(Tier 1: 동시 13미팅)와 STT CPU이지 서버 스레드 모델이
아니다. 의존성 0 원칙과 배포 단순성(개인 VPS에서 pip 없이)이 실제 가치이고,
웹 노출면 방어는 Caddy가 앱보다 잘한다. 동시 미팅 수십을 넘겨 Tier 2+와
STT GPU가 필요해지는 시점에 asyncio 이관을 재평가한다.


---

## 9. 자율 고도화 스프린트 — 감사 기반 갭 해소 (2026-08-22)

Phase 0 감사([AUDIT.md](./AUDIT.md))에서 수용 기준 6개 대비 갭 3건을 식별하고
전부 구현·검증했다. 과정에서 검색 품질 회귀 1건을 추가로 발견해 고쳤다.

### 9.1 Before / After

| 항목 | Before | After |
|---|---|---|
| 미팅 종료 질문 리스트 | 비즈니스 후속 질문만 | + **🎓 진옥 선생님께 물어볼 것** — 못 알아들은 관용구·못 만든 문장을 영어 원문 인용과 함께 수업용 질문으로 (미팅의 막힌 지점 → 다음 수업 커리큘럼) |
| 데스크톱(1280px) | 자막·카드가 1,200px 전폭으로 늘어져 흘끗 읽기 불가 | 660px 중앙 칼럼 — 사이드 패널 폭과 동일한 읽기 폭 (390px 회귀 없음) |
| 번역 대기 | 빈칸이었다가 갑자기 채워짐 | 깜빡이는 `···` — "없는 것"과 "오는 중"이 구분됨 |
| 한국어 검색 순위 | '어요' 같은 어미 조각이 매칭을 오염 — 감사 질의에 인사 노트가 상위 | 어미·조사 2-gram 40여 개 제외 + 전체 재색인 — 감사 노트 1·2위 복귀 |
| E2E 범위 | 미팅 종료 흐름 미포함 | ⏹ 종료 → 요약·적재·복습·수업 질문까지 **전체 여정** 검증 (34체크) |

### 9.2 수용 기준 최종 판정 (증거)

| # | 기준 | 판정 | 증거 |
|---|---|---|---|
| 1 | 자막 interim/final + 2초 내 번역 | ✅ | final→번역 표시 97ms(모의) · interim 점선+커서+라벨 |
| 2 | 롤링 요약 (10버퍼·3문장/30초) | ✅ | E2E "견적가가 경쟁사보다 높다는 반론" 배너 |
| 3 | 4버튼 RAG · top3 · 15단어 · 3초 · 📚 | ✅ | 클릭→표시 123ms(모의) · 15단어 100% · 뱃지는 실사용 시만 |
| 4 | 퀵 번역 동일 파이프라인 | ✅ | 근거가 수업 노트로 표시됨 (E2E) |
| 5 | 표현 10 + SRS 1/3/7 + 수업용 질문 | ✅ | 이번 스프린트에서 수업용 질문 추가 완료 |
| 6 | Lighthouse 90+ · 390px · 상태 UI | ✅ | **모바일·데스크톱 모두 100×4** · 대기 표시 추가로 상태 UI 완비 |

전체 회귀: E2E 34체크 통과(콘솔·네트워크 오류 0) · rag-eval **10/10** ·
웹 계층 16/16 · Lighthouse 두 프리셋 전부 100.

### 9.3 남은 기술부채

- 실 Claude에서의 형식 준수율(15단어·EN/KR)·실지연 미실측 — 모의 기준 수치임
- 쿼터가 호출 수 기준(토큰 아님), 세션 개별 무효화 불가(무상태 토큰의 트레이드오프)
- Wake Lock 미적용(모바일 화면 꺼짐 시 마이크 정지), iOS 실시간 인식은 플랫폼 한계

### 9.4 다음 스프린트 제안 3가지

1. **실전 리허설 스프린트** — 실 Claude 키 + 실제 유튜브 영어 회의 영상으로
   30분 모의 미팅 1회. 측정: 번역 p95, 제안 첫 토큰, 15단어 준수율, 뱃지 발동률,
   복습 JSON 파싱 성공률. 이 수치가 나와야 "다음 미팅에서 의지할 수 있다"가 증명된다.
2. **약점 드릴 루프 완성** — 복습 카드에서 3회 틀린 표현을 자동으로 '약점'으로
   승격해 다음 미팅 제안에서 우선 노출 + 수업 질문에 반영 (지금은 SRS에서 끝난다).
3. **미팅 프리셋** — 미팅 시작 전에 고객사·안건을 한 줄 입력하면 해당 도메인
   청크를 미리 가열(핀)해 첫 제안부터 맥락이 실리게 (지금은 대화가 쌓여야 맥락이 생긴다).


---

## 10. 실시간 리플레이 테스트 (2026-08-22)

즉답 모의로는 실시간성을 증명할 수 없어, 조건을 실전에 최대한 붙여 재측정했다.

**테스트 조건** — ① 실제 대화 속도: 23발화를 발화 간 5~12초 간격으로 약 3.5분간
재생 (즉시 주입 아님) ② 실제 브라우저(390px)에서 전체 파이프라인·렌더링 경유
③ LLM은 **실 Claude 지연 프로필 재현** 공급자 — Haiku 상당 TTFT 0.55s·110tok/s,
Sonnet 상당 TTFT 0.95s·70tok/s (실 API 아님 — 이 환경엔 키가 없다).

| 측정 항목 | 결과 | 수용 기준 |
|---|---|---|
| 자막: final → 한국어 표시 (상대 발화 13건 전부) | **p50 0.57s · p95 0.61s** | 2초 이내 ✅ |
| 제안: 버튼 클릭 → 첫 옵션 표시 (3의도) | **0.98~1.09s** | — |
| 제안: 클릭 → 2안+한국어 완성 | **1.55~1.62s** | 3초 이내 ✅ |
| 한→영 퀵 번역 완성 | 1.37s | 동일 파이프라인 ✅ |
| 요약 배너 | "견적가와 총소유비용 논의"로 갱신 확인 | ✅ |
| ⏹ 종료(요약+적재+복습+수업질문) | 5.3s | 실시간 아님 — 허용 |
| JS 힙 (3.5분 미팅) | 1.4 → 3.1MB | 누수 없음 |
| JS 오류 | 0 | ✅ |

측정 결함 1건을 스스로 발견·수정: 최초 계측이 "⏳ 생성 중…" 플레이스홀더를
첫 표시로 오인 → 실제 첫 문장 기준으로 재측정한 것이 위 수치다.

**이 환경에서 검증 불가로 남은 것(정직 고지)** — 실제 마이크 입력(샌드박스에
오디오 장치 없음), 실 Claude API(키 없음), 실 Whisper STT(모델 저장소가 네트워크
정책상 차단 — faster-whisper 설치와 합성 음성 준비까지는 완료). 이 세 가지가
배포 첫날 실측 목록이다.

→ 이 실측은 준비돼 있다: 맥북에서 `bash meeting-copilot/doctor.sh`(사전 점검) →
`bash meeting-copilot/smoke.sh`(구간별 측정)를 실행하면 결과가 이 문서 10.1절에
자동 기록된다. 절차는 [FIELD-TEST.md](./FIELD-TEST.md).


---

## 11. Gemini 전환 + 무료 티어 RPM 설계 (2026-08-22)

기본 공급자를 Claude → **Gemini(무료 티어)**로 전환하고, 분당 10~15회 한도
안에서 실시간성이 유지되게 호출 설계를 다시 짰다. 수용 기준 G1~G9는 PLAN.md.

### 11.1 설계 요약

| 장치 | 내용 |
|---|---|
| 모델 티어링 | 번역·한줄요약 = Flash-Lite(15 RPM/1,000 RPD) · 제안·전체요약·자산화 = Flash(10 RPM/250 RPD). 전부 env, 하드코딩 0 |
| 번역 배칭 | 예산 여유(잔여 3+) 시 **즉시 전송으로 지연 유지**, 빠듯하면 2~3문장 또는 4초 = 1호출. 번호 매핑 줄단위 스트리밍이라 첫 문장 번역은 배치 완료 전에 표시 |
| 요약 완화 | 3발화/30초 → **5발화/60초**, fast 레인 잔여 ≤4면 이번 주기 건너뜀(최후순위) |
| 우선순위 | 퀵 리액션(클릭) 최우선 → 번역 → 요약. 투기 생성·자동 제안은 여유 시만, 일일 70% 소진 시 자동 제안 중단(클릭은 계속) |
| 429 | 대안 공급자 없으면 지수 백오프+지터(최대 2회) — 몰아치기 18회 실측 14회 회복. **RPD 소진은 구분 감지** → "오늘 무료 한도 소진 — 약 N시간 후(태평양 자정) 리셋" 배너 |
| 가시성 | 상단 ⚡칩: 분당/일일 실사용 (서버 /api/usage — 재시작에도 일일 카운트 영속, PT 날짜 기준) |
| 프라이버시 | 🎓연습/🔒실미팅 모드. 실미팅+무료 티어면 시작 전 "대화가 모델 학습에 사용될 수 있습니다" 경고 (유료는 GEMINI_TIER=paid 선언) |

### 11.2 15분 연속 대화 시뮬레이션 (핵심 검증)

조건: 발화 120개를 분당 8개(7.5초 간격)로 **실시간 15.2분** 주입 + 90초마다
퀵 리액션 클릭 + 자동 제안·요약 가동. 모의 Gemini가 무료 한도(Flash-Lite 15
RPM · Flash 10 RPM)를 **실제로 강제**(초과 시 429 반환). 문장은 전부 고유
(번역 캐시가 부하를 흡수하지 못하게).

| 항목 | 결과 | 판정 |
|---|---|---|
| 429 응답 | **0건** | ✅ |
| 상대 발화 번역 표시 | **80/80** (실패·누락 0) | ✅ |
| 번역 표시 지연 | p50 423ms · p95 433ms | ✅ (기준: 여유 시 2초, 배칭 시 7초) |
| 제안 호출 | 80회 — Flash 10 RPM 안에서 소화 | ✅ |
| JS 오류 | 0건 | ✅ |

관찰: 분당 8발화 수준에서는 fast 레인 사용이 ~6/13에 머물러 **배칭 없이 즉시
전송이 유지**됐다(지연 무손실). 배칭은 압박 상황 테스트에서 별도 검증
(3문장→1호출, 버블 정확 매핑). 시뮬에서 자동 제안 5.3회/분 = RPD 250이
47분에 소진됨을 발견 → 일일 70% 소진 시 자동 제안을 끄는 감쇠를 추가했다.

### 11.3 회귀 (Gemini 모의 체제)

rag-eval 10/10 · E2E 전부 통과(오류 0) · web_qa 16/16 · smoke 4단계
(번역 453ms·제안 1,463ms — 신규 기준 통과) · doctor Gemini 실호출+티어 안내.
E2E 직후 smoke의 제안이 429로 실패한 사례는 **mock의 한도 강제가 작동한 것**
— 60초 창 배수 후 재실행 통과.

### 11.4 미검증 (정직 고지)

실 Gemini API 미호출(이 환경 키 없음 — 지연·한도 수치는 공시값 기반 모의).
맥북에서 `doctor.sh`(실 호출 1회) → `smoke.sh` → FIELD-TEST 절차가 그대로
실측을 채워준다.


<!-- FIELD-RESULTS:START -->
### 10.1 내 맥북 실측 (smoke.sh 자동 기록 — 2026-08-22 05:55, vm)

아래는 §10의 "지연 프로필 재현"을 대체하는 **실측값**이다.

| 구간 | 실측 | 수용 기준 | 판정 |
|---|---|---|---|
| 마이크 → STT 인식 | --skip-mic | — | ⏭ 미측정 |
| 번역 (final → 한국어 완료) | 453ms | 2000ms 이내 | ✅ |
| RAG 검색 (질의 2건) | - | — | ✅ |
| 퀵 리액션 (클릭 → 2안 완성) | 1420ms | 3000ms 이내 | ✅ |

미달(🔴) 구간은 [FIELD-TEST.md](./FIELD-TEST.md)의 증상별 대응표를 참조.
<!-- FIELD-RESULTS:END -->

---

## 12. 최종 스프린트 — 8/27 인터뷰 실전 투입 준비 (2026-08-22)

목적: 8/27 영어 인터뷰(HR 스크리닝, 1:1 화상)에서 보조 도구로 실사용 가능한 상태.

### 12.1 RAG 인터뷰 시드 (작업 1)

`interview-corpus.json` **70개** 추가 — 전 항목 [영어 문장 / 한국어 뜻 / 상황태그]
구조에 검색 다리(triggers) 포함, 단어 나열 없이 전부 말할 수 있는 문장:

- **iPaaS/Workato 도메인 30**: workflow automation 가치, iPaaS 한 줄 정의, 커넥터,
  로우코드, 레시피, land-and-expand, build-vs-buy 반론 대응, ROI 프레임,
  IT·현업 동시 설득, cloud MSP 배경 연결 등
- **인터뷰 표현 40**: 자기소개("I am a B2B sales hunter focused on opening new
  enterprise accounts…"), 되묻기/시간 벌기("Could you rephrase that?",
  "That's a great question — let me think…"), 역질문(팀 구조·온보딩·성공 기준),
  클로징(관심 표현·다음 단계·감사)

색인 용어집 70 → **140**. rag-eval에 인터뷰 시나리오 5쿼리(영어 질문 3 +
한국어 퀵번역 2) 추가 → **15/15 (100%)**, 임베딩 없이 키워드만으로.

### 12.2 인터뷰 톤 프리셋 (작업 2)

- **🎤 인터뷰 프리셋** (연습/실미팅 모드와 별개): 4버튼이
  [👍 동의 / ➕ 부연 설명 / 🔁 되묻기 / 💬 역질문]으로 — 면접에 부적절한
  '반박' 제거, 시간 벌기는 보조줄 유지. 새로고침에도 유지.
- **생성 톤 전환**: 미팅(회사 대 회사) ↔ 인터뷰(후보자 1인칭, 자신 있고
  따뜻하게, 협상 어휘 금지) — 프롬프트 헤더·규칙이 프리셋에 따라 바뀐다.
- **퀵 번역 우선 노출**: 인터뷰 프리셋에서 입력창이 버튼 위 + 강조 테두리,
  `/` 또는 Cmd/Ctrl+K로 어디서든 포커스. 인터뷰에서 가장 쓸 기능이라는 판단.
- **400px 세로 검증**(화상통화 옆): 오버플로 0, 헤더 106→70px 압축, 스크린샷 확인.

### 12.3 퀵 리액션 샘플 5문항 (눈 판정용)

HR 스크리닝 전형 질문 5개를 인터뷰 프리셋으로 실행한 결과.
**📎 검색 근거는 실제 파이프라인 출력**이고, 생성문 중 1안은 검색 자료를
그대로 활용한 것(모의 공급자가 자료 문장을 선택) — **실 Gemini의 문장 품질은
맥북 필드 테스트에서 확인**해야 한다(이 환경엔 실 키 없음).

| 면접관 질문 | 검색된 내 자료 (top 3) | 생성 1안 (자료 활용) |
|---|---|---|
| Tell me about yourself. | **intro one-liner** · buy time classic · deal example frame | "…B2B sales hunter focused on opening new enterprise accounts in the cloud market" |
| Why are you interested in joining Workato? | **why this company** · why automation now · recipe concept | "I want to sell a product that changes how customers work, not just…" |
| Walk me through how you open a brand-new enterprise account. [부연] | **enterprise sales cycle** · deal example frame · circle back | "I am comfortable running six-to-twelve-month cycles with multiple stakeholders" |
| What do you know about our platform and the iPaaS space? | **iPaaS in one line** · observability · integration plus automation | "The real differentiation is combining integration and automation in a single…" |
| Do you have any questions for us? [역질문] | **follow-up offer** · next steps · landing the plane | "Happy to share more detail on any of my deals in a follow-up" |

→ 5문항 전부 **그 질문에 맞는 준비 자료가 top-3에 소환**됐다. 검색이 맞으면
생성은 자료를 따라간다 — 이 표가 8/27에 앱이 건네줄 커닝페이퍼의 실체다.

### 12.4 실측 피드백 루프 (작업 3)

- FIELD-TEST 4절 "인터뷰 리허설": Preply/화상 수업에서 확인할 5항목
  (힐끗 볼 여유·퀵 리액션 실사용·시야 방해·폰트 크기·종료 자산화 품질) —
  기준은 "기능이 도는가"가 아니라 "실전에서 의지되는가". 미달 시 현실적
  대안(퀵 번역 단독 전략)까지 명시.
- smoke.sh 하단에 **복붙용 진단 블록**: OS·Python·공급자·모델 티어링·티어·
  로컬 STT·커밋 해시 + 단계별 결과 JSON. "이 블록을 Claude Code에 그대로
  붙여넣으세요" — 환경 되묻기 왕복 제거.

### 12.5 전체 회귀 (증거)

| 검증 | 결과 |
|---|---|
| rag-eval (인터뷰 5쿼리 포함) | **15/15 (100%)** |
| E2E 전체 여정 (인터뷰 프리셋 검사 포함 별도 6체크) | 전부 통과 · 콘솔 오류 0 |
| web_qa (인증·격리·전송) | 16/16 |
| smoke 4단계 | 번역 454ms · 제안 1,420ms — 기준 내 |
| 15분 시뮬 (무료 한도 강제) | **429 0건 · 번역 80/80 · p95 437ms · JS 오류 0 — 완주** (프리셋 코드 반영 후 재실행) |

과정에서 잡은 것 2건: ① 한도 강제 mock이 기능 E2E까지 429로 흔들던 것 →
mock을 `tests/mock_gemini.py`로 승격하고 한도를 env로 분리(기능 테스트는 완화,
rpm-sim은 무료 실값 강제) ② rag-eval을 오염시키던 시뮬 잔여 트랜스크립트 정리.

### 12.6 8/27 전까지 남은 것 (사용자 액션)

1. 맥북에서 `doctor.sh` → `smoke.sh` (실 Gemini 키 — 실측이 10.1절에 자동 기록)
2. 유튜브 필드 테스트 30분 (FIELD-TEST 1~3절)
3. **Preply 수업에서 인터뷰 리허설 1회** (FIELD-TEST 4절 — 이게 최종 관문)
4. 자기소개·딜 스토리를 '자료' 탭에 본인 문장으로 추가하면 제안이 더 개인화된다


## 13. Out-of-Corpus 대응 — 시드 밖 질문에서의 답변 품질 (2026-08-22)

rag-eval 15/15는 "시드와 매칭되는 질문"만 검증했다. 실전 인터뷰의 절반은 시드에
없는 질문이므로, **검색이 빗나가거나 부분 적중일 때도 말할 수 있는 답**이 나오는지
3계층 15문항(A 시드 변형 / B 시드 인접 / C 완전 이탈)으로 검증했다
(`tests/ooc-eval.ts` = 케이스 단일 소스, `tests/ooc_eval.py` = 실행·기록 러너).

**방어선 3겹 (backend/rag.py · prompts.py):**

1. **관련성 스코어 컷** — BM25 히트마다 "질의 어절이 몇 개나 매칭됐는가"(`match_terms`)를
   세고, 어절 2개 미만이면서 의미(벡터) 히트도 아닌 자료는 프롬프트에서 버린다.
   B·C 계층에서 무관 시드를 억지로 끼워넣는 것을 차단.
2. **고정 프로필 블록** — 검색 결과가 0이어도 시스템 프롬프트에 후보 프로필
   (B2B enterprise sales hunter, cloud MSP, 8+ years, new business focus)을 항상 주입.
   '자료' 탭에 `내 프로필` 제목의 노트를 넣으면 그 내용이 기본값을 대체한다.
3. **하드 룰** — "I'm not sure" 류 회피성 문구 금지, 느슨하게만 관련된 자료의
   강제 인용 금지를 프롬프트에 명시. 평가기가 정규식으로 위반을 잡아낸다.

**이 과정에서 찾은 검색 결함 3건 (수정 완료):**

- `me/my` 등 대명사가 불용어가 아니어서 "Tell **me** about..."의 me가 매칭 어절로
  집계돼 무관 시드(buy time)가 컷을 통과 → 대명사류를 `_STOP`에 추가.
- `what/how` 등 의문사도 같은 문제("**What** do you do outside work") → 의문사 추가.
- 의문사 제거로 rag-eval 1건("How would the migration actually roll out?")이 회귀 →
  rollout plan 등 트리거 7건에 패러프레이즈 보강 후 15/15 복구.

회귀 확인: rag-eval **15/15**, ooc-eval **15/15**, E2E 전부 통과. 아래 표는
모의 LLM 기준이며, 맥북에서 실 키로 서버를 띄우고 `python3 tests/ooc_eval.py`를
다시 실행하면 같은 표가 실측 생성문으로 갱신된다.

<!-- OOC-RESULTS:START -->
### 13.1 결과 표 (ooc_eval.py 자동 기록 — 2026-08-29 04:46 · 공급자: gemini (gemini-3.6-flash))

| 계층 | 면접관 질문 | 검색 근거 (관련성 컷 통과분) | 생성 2안 | 판정 |
|---|---|---|---|---|
| A | Walk me through your background. | 면접 프로세스 역질문 (반드시 실행)<br>자기소개 — 표준 골격 (08 캐노니컬, 담백한 톤)<br>연봉 — 되물음 대응 + 밴드 반응 3종 (2차) | Rather than anchoring on a figure, what matters. So let me share one example from a recent | ❌ 기대 시드 미검색 (실제: Notion 수업 노트: 면접 프로세스 역질문 (반드시 실행), Notion 수업 노트: 자기소개 — 표준 골격 (08 캐노니컬, 담백한 톤), Notion 수업 노트: 연봉 — 되물음 대응 + 밴드 반응 3종 (2차)) |
| A | What brings you here today? | why this company<br>career move logic | This role connects my cloud background with where. So let me share one example from a rece | ✅ |
| A | How do you land new logos? | 첫 90일 계획 패턴<br>확장 전략 — 작은 딜에서 시작하는 이유<br>딜 스토리 G — 헌팅 철학 | I respect the onboarding process, but I'd run. So let me share one example from a recent d | ❌ 기대 시드 미검색 (실제: Notion 수업 노트: 첫 90일 계획 패턴, Notion 수업 노트: 확장 전략 — 작은 딜에서 시작하는 이유, Notion 수업 노트: 딜 스토리 G — 헌팅 철학) |
| A | How would you explain integration platforms to a beginner? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ❌ 기대 시드 미검색 (실제: 없음) |
| A | What would you ask us about how the team works? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ❌ 기대 시드 미검색 (실제: 없음) |
| B | What's your experience with Salesforce integration specifically? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ✅ |
| B | How do you handle a deal going dark after the proposal? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ✅ |
| B | What are your salary expectations for this position? | 연봉 질문 — 밴드 역질문 (1차 표준 대응)<br>연봉 — 되물음 대응 + 밴드 반응 3종 (2차) | Rather than anchoring on a figure, what matters. So let me share one example from a recent | ✅ |
| B | How do you split your time between hunting and account management? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ✅ |
| B | Have you ever sold against an incumbent vendor with a locked-in contract? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ✅ |
| C | What do you do outside work for fun? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ✅ |
| C | Tell me about a time you failed at something. | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ✅ |
| C | Why are you leaving your current role right now? | 이직 사유 — 시그니처 답변 (Why Workato)<br>GitLab 판매 실적 — 엔터프라이즈 플랫폼 셀링 증거<br>연봉 — 되물음 대응 + 밴드 반응 3종 (2차) | I recently closed Nexus Community, and ING Story. So let me share one example from a recen | ❌ 무관 시드 인용 의심: Notion 수업 노트: 이직 사유 — 시그니처 답변 (Why Workato), Notion 수업 노트: GitLab 판매 실적 — 엔터프라이즈 플랫폼 셀링 증거, Notion 수업 노트: 연봉 — 되물음 대응 + 밴드 반응 3종 (2차) |
| C | How do your colleagues usually describe you? | — (프로필 폴백) | That's a good question. / Let me think about that for a second. | ✅ |
| C | Where do you see yourself in five years? | 자기소개 — 표준 골격 (08 캐노니컬, 담백한 톤) | quickly but struggle to scale into production. So let me share one example from a recent d | ✅ |

**10/15** (A 1/5 · B 5/5 · C 4/5). 계층 기준 — A: 시드 검색·활용 / B: 무관 시드 강제
인용 없이 생성 / C: 검색 0이어도 프로필 기반 답변, 회피성 문구 금지.
<!-- OOC-RESULTS:END -->

## 14. Spoken English 전환 — "읽는 영어"에서 "말하는 영어"로 (2026-08-22)

13장까지의 생성문·시드는 문어체·고급 어휘로 치우쳐 있었다. 실전에서 긴장한
비원어민이 **즉시 입으로 뱉을 수 있는** CEFR B1~B2 구어체가 기준이다.
지시(프롬프트)만으로는 모델이 미끄러지므로, **평가기로 강제**했다.

### 14.1 발화 스타일 규칙 (프롬프트 명시 + `tests/speakability.ts`로 검증)

1. 문장당 12단어 이내 (기존 15 → 12 강화)
2. 축약형 필수: I'm / I've / that's / don't (I am, do not 검출 시 실패)
3. 쉬운 동사: utilize→use, elaborate→say more, demonstrate→show, acquire→get/win
4. 구어 연결어: So / Actually / Basically (Furthermore·Moreover·In addition 금지)
5. 관계대명사 중첩 금지 — 한 문장 한 정보, 길어지면 두 문장
6. 어려운 발음(entrepreneurship, differentiation 등) 대체
7. 예외: iPaaS·workflow·integration·pipeline 등 도메인 필수 용어는 유지

### 14.2 2안 역할 분리 — Safe / Rich

| | 역할 | 상한 | UI |
|---|---|---|---|
| **1안 Safe** | 짧고 확실한 문장 — 긴장 상태에서 그대로 읽는다 | 문장당 **9단어** | 위에, 더 크게 |
| **2안 Rich** | 내용 한 스푼 추가 (숫자·예시) | 문장당 **12단어** | 아래, 보조 |

### 14.3 시드 70개 전면 리라이트 — Before/After (소리 내어 읽고 판정)

| 시드 | Before (문어체) | After (구어체) |
|---|---|---|
| intro one-liner | I am a B2B sales hunter focused on opening new enterprise accounts in the cloud market. | I'm a B2B sales hunter. I open new enterprise accounts in cloud. |
| current role summary | At a cloud MSP, I hunt new business and build accounts from the first cold touch to close. | I hunt new business at a cloud MSP. I build accounts from cold call to close. |
| why this company | I want to sell a product that changes how customers work, not just where their servers run. | I want to sell a product that changes how customers work. Not just where their servers run. |
| loss lesson | I lost a deal by pitching too early, and now I never present before I understand the pain. | I once lost a deal by pitching too early. Now I never present before I understand the pain. |
| integration plus automation | The real **differentiation** is combining integration and automation in a single platform. | We combine integration and automation in one platform. That's the real edge. |

한국어 뜻·상황태그·검색 트리거는 그대로 두고 영어 문장만 바꿨다
(검색 다리가 깨지지 않도록 — 14.4에서 회귀로 확인).

### 14.4 Speakability 평가기 + 전체 회귀 (증거)

`tests/speakability.ts` — 문장당 단어 수(≤12, 생성 1안은 ≤9) · 미축약 표현 ·
금지어 28종(어간 매칭)을 자동 검출한다. 금지어·미축약 목록은 이 파일 한 곳에만
있고, ooc-eval(.ts/.py)과 E2E가 같은 규칙을 가져다 생성문에도 적용한다.

| 검증 | 결과 |
|---|---|
| speakability — 시드 70개 전수 | **위반 0** ✅ |
| rag-eval (리라이트 후 검색 회귀) | **15/15** ✅ |
| ooc-eval (구어체 검사 연결 후) | **15/15** (A 5/5 · B 5/5 · C 5/5) ✅ |
| E2E (Safe≤9·Rich≤12 검사로 강화) | **전부 통과 · 오류 0** ✅ |
| 15분 시뮬 (무료 한도 강제) | **429 0건** · 번역 80/80 · p95 434ms ✅ (15.2분 실측) |

13.1의 15문항 표도 리라이트된 생성문으로 자동 갱신됐다 (모의 LLM 기준 —
실 키로 `python3 tests/ooc_eval.py` 재실행 시 실측으로 교체).

## 15. 최종 전수 성능테스트 (2026-08-22)

출시 전 최종 관문으로 전체 스위트를 한 번에 재실행했다. 기능·품질 평가는 완화
한도 모의 Gemini로, 마지막 15분 시뮬은 **무료 티어 실제 한도(15/10 RPM) 강제**로.

### 15.1 품질·기능 (전부 통과)

| 스위트 | 결과 |
|---|---|
| speakability — 시드 70개 전수 | 위반 **0** ✅ |
| rag-eval 검색 15케이스 | **15/15** ✅ |
| ooc-eval 3계층 15문항 (py+ts 러너 각각) | **15/15** ✅ |
| web_qa 웹 계층 16체크 (keep-alive·타임아웃·413·스트림 프레이밍) | **16/16** ✅ |
| E2E 34체크 ×2회 연속 (Playwright 390×844) | **전부 통과 · 콘솔/네트워크 오류 0** ✅ |
| Lighthouse 모바일/데스크톱 | **전 카테고리 100 / 100** ✅ |

### 15.2 성능 실측 (색인 158청크 · 모의 공급자)

| 항목 | 수치 |
|---|---|
| 검색 단독 30회 | p50 **2ms** · p95 4ms |
| 검색 동시 4건 / 8건 | p50 6ms / 14ms · 오류 0 |
| 제안 종단(스트림 완료) 12회 | p50 1,378ms · p95 1,466ms · 실패 0 (모의 flash TTFT 0.8s 포함) |
| 번역 12회 | p50 454ms · p95 460ms · 실패 0 |
| 혼합 부하 (동시 8커넥션 × 검색+제안+번역 ×3) | 24호출 오류 **0** |
| 서버 RSS (부하 후) | 5MB |

### 15.3 한도 방어 (무료 티어 강제)

- **RPD 소진 감지·복구**: 모의 PerDay 429 주입 → "오늘 무료 한도 소진 — 태평양
  자정 리셋" 안내 노출, 해제 후 다음 호출부터 즉시 복구 ✅
- **15분 시뮬 (120발화 · 7.5초 간격)**: 429 **0건** · 번역 표시 **80/80** ·
  p95 442ms · JS 오류 0 ✅

### 15.4 테스트가 잡아낸 것 — 상태 격리 교훈

첫 15분 시뮬은 번역 0/80으로 실패했다. 원인은 앱 결함이 아니라 **직전 RPD 소진
테스트가 남긴 상태**: 서버가 "오늘 한도 소진"을 기억하고(설계 의도) 클라이언트
거버너가 설계대로 번역 호출을 전부 멈춘 것이다. 배너에 "⛔ 오늘 무료 한도 소진"이
정확히 떠 있었고 429도 0건 — 즉 **한도 방어 로직이 정상 동작한다는 역방향 증거**다.
서버 재시작 + 사용량 카운터 초기화 후 재실행에서 위 15.3 결과로 통과했다.
실환경 함의: RPD가 실제로 소진된 날은 재시작해도 카운트 파일 기준으로 차단이
유지되며, 태평양 자정에 자동 리셋된다.

## 16. API 장애 대응 — 진단 계측 + 단일 게이트웨이 버스트 제어 (2026-08-22)

유료 키에서도 장애가 발생했다 — 무료 RPD만이 원인이 아니라는 뜻이다. 용의자는
① 순간 버스트의 분당 한도 초과 ② 지출 기반 한도 ③ 429 후 즉시 재시도 폭풍
④ 공급자측 일시 5xx의 한도 초과 오인. **추측으로 고치지 않기 위해 계측을 먼저
넣고**, 버스트가 구조적으로 불가능한 단일 게이트웨이로 호출 경로를 재편했다.

### 16.1 장애 계측 (작업 1 — 진단이 먼저다)

- 모든 실 API 시도(재시도 포함)가 `logs/api-trace.jsonl`에 남는다:
  시각 · 호출 유형 · 모델 · **직전 60초 발사 수** · **동시 진행 수** · HTTP 상태 ·
  오류 원문 · Retry-After · 재시도 차수 · 큐 대기·지연 ms
- **429와 500/503을 구분 집계** — 429 다발이면 한도/버스트 문제(설정을 만진다),
  5xx 다발이면 공급자측 장애(기다리면 지나간다 — 키를 만질 필요 없음)
- `bash incident.sh [분]` → 오류 구간을 15초 창으로 묶어 **붙여넣기용 진단 블록**
  출력: `[13:51:50~13:51:57] 60초 발사 8건 · 동시 1 → 429 2건 (Retry-After: 1s)` 식

### 16.2 단일 게이트웨이 (작업 2 — 구조적 수정)

번역/제안/요약이 각자 호출하던 것을 전부 한 관문으로:

```
번역 ─┐                       ┌─ 동시 실행 상한 2 (스트림 종료까지 슬롯 점유)
제안 ─┼─▶ [우선순위 큐] ─▶ [레인별 토큰 버킷] ─▶ [브레이커] ─▶ Gemini API
요약 ─┘    클릭 제안 0         main(제안·요약):       연속 429 3회
자산화      번역·최종요약 1      무료 8 · 유료 60/분    → 30초 차단
           투기·자산화 2       fast(번역): 13·120/분   (큐는 유지, 배너
           한줄요약 3          버킷 용량 2 = 버스트 상한  카운트다운 표시)
              │
              └─ 드롭: 한줄요약 5초↑ · 투기 제안 15초↑ · 좀비 클릭 60초↑
```

- 재시도도 같은 토큰을 소비한다 — **재시도 폭풍이 구조적으로 불가능**
- 투기 생성 보수화: 분당 예산 **잔여 50%+**일 때만 발사 (기존 "잔여 3+")
- 검증이 찾아낸 결함 2건: ① 클라이언트가 abort한 투기 제안이 큐에서 250초
  살아남아 번역까지 밀어냄 → 유형별 TTL 드롭. ② 단일 전역 버킷에서는 값싼
  번역(lite)이 제안(flash)과 토큰을 경쟁해 15분 시뮬 p95가 8초로 밀림(실측)
  → 실제 한도가 모델별이므로 버킷도 레인별로 분리 (fast 13/분 · main 8/분)

### 16.3 재시도 규율 (작업 3)

| 상황 | 동작 |
|---|---|
| 429 | Retry-After 헤더 **무조건 준수** (없으면 지수 백오프+지터) · 최대 2회 · 초과 시 그 항목만 실패 표시 |
| 429 연속 3회 | 서킷 브레이커 30초 (신규 발사 차단·큐 유지) · 배너 "⏸ 호출 급증으로 잠시 대기 중 (N초)" |
| 500/502/503 | 짧은 백오프 1회 재시도 → 실패 시 그 항목만 스킵 · 공급자 제외도 5초만 (한도 문제로 오인 금지) |
| RPD 소진 | 재시도 없이 즉시 포기 + PT 자정 리셋 안내 (기존 동작 유지) |

### 16.4 시나리오 검증 (`tests/resilience-sim.mjs` — 전부 mock)

| 시나리오 | 결과 |
|---|---|
| A. 버스트: 10발화/5초 + 버튼 연타 + 퀵 번역 | **429 0건** · 60초 발사율 8(=한도) 평활화 · 번역 7/7 표시 ✅ |
| B1. 429 전면(p=1.0) 주입 | 연속 429 3회 → **브레이커 발동**(trips=1) · 배너 "⏸ 호출 급증으로 잠시 대기 중 (23초)" · 해제 후 첫 클릭 회복 ✅ |
| B2. 429 무작위 30% 주입 | 클릭 제안 생존 **8/8** (재시도 6회가 429 7건 흡수) · 전체 중단 없음 ✅ |
| C. 503 간헐(p=0.5) 주입 | 5xx 별도 집계 · 항목별 스킵 · JS 오류 0 · 해제 후 정상 복귀 ✅ |
| 종합 | 클라이언트로 새어나간 429 **0건** · JS 오류 0 · 게이트웨이 통계: 발사 25 · 재시도 8 · 드롭 24 · 브레이커 1회 |

### 16.5 회귀 (게이트웨이 활성 상태)

| 검증 | 결과 |
|---|---|
| rag-eval | 15/15 ✅ |
| ooc-eval | 15/15 ✅ |
| speakability 시드 70 | 위반 0 ✅ |
| E2E 34체크 | 전부 통과 · 오류 0 ✅ |
| 15분 시뮬 (무료 한도 + 게이트웨이 기본 8/분) | 429 **0건** · 번역 80/80 · p95 **437ms** ✅ (레인 분리 전 전역 버킷은 p95 7,988ms — 16.2의 결함 ②) |

실 유료 키 사용 시: `GEMINI_TIER=paid`로 게이트웨이 발사 상한이 60/분이 되고,
장애가 다시 나면 `bash incident.sh 60` 출력을 그대로 붙여넣으면 된다 —
429인지 5xx인지, 그때 발사율·동시성이 얼마였는지가 한 블록에 나온다.

## 17. 출시 전 CTO 리뷰 보완 — 실키에서만 터지는 유형 2건 (2026-08-23)

필드 테스트 직전 리뷰에서 "모의 환경이 절대 재현하지 않는" 실패 유형 2건을
찾아 코드로 막았다. 둘 다 실패 시나리오가 "8/27 인터뷰 도중"인 것들이다.

| # | 결함 | 수정 | 증거 |
|---|---|---|---|
| P0-1 | 실 Gemini는 안전 필터·빈 candidate일 때 **HTTP 200 + 텍스트 0자**를 준다. 기존 코드는 이걸 성공으로 처리 → 제안 카드가 조용히 빈다 | 빈 응답을 실패로 분류(트레이스 status 204) → 짧은 백오프 1회 재시도 → 반복 시 그 항목만 명시적 실패. mock에 `{"mode":"empty"}` 주입 모드 추가 | p=1.0 주입 → 침묵 대신 "빈 응답 반복 — 이 항목만 건너뜀" · p=0.5 → 재시도가 흡수 (트레이스 204→200 확인) |
| P0-2 | 대화 기록이 브라우저 메모리에만 있어 **새로고침·탭 크래시 = 미팅 전체 유실** | 발화·번역 갱신마다 1초 디바운스로 localStorage 백업, 재접속 시 "이어서 복원할까요?" → LLM 재호출 0회로 복원, ⏹ 정상 종료 시 삭제 | 4발화+번역 2건 → 새로고침 → **4/4 복원 · 번역 2/2 · 추가 LLM 호출 0** · 종료 후 백업 삭제 확인 |
| P2 | `logs/api-trace.jsonl` 무한 증가 | 기동 시 5MB 초과면 최근 2,000건만 유지 | — |

회귀: rag-eval 15/15 · ooc-eval 15/15 · speakability 위반 0 · E2E 전부 통과(오류 0).

의도적으로 **고치지 않고 테스트로 넘긴 것**: 음성 인식 오인식 내성(유튜브 필드
테스트에서 실측이 정확), 실키 지연(smoke.sh가 자동 기록), 현장 네트워크(리허설).

## 18. 최종 전수 검증 라운드 — "꼼꼼 모드" (2026-08-23)

출시 직전 마지막 전수 실행. 이전 라운드들의 교훈(상태 오염 → RPD류 테스트는
맨 끝, 콜드 스타트 미측정)을 순서에 반영해 사각지대까지 훑었고, **이번에도
3건이 나왔다 — 전부 수정·재검증 완료.** 그리고 그중 2건은 "이전 테스트가
왜 놓쳤는가"까지 답이 나온, 테스트 인프라 자체의 결함이었다.

### 18.1 이번 라운드가 잡은 것

| # | 발견 | 원인 | 수정 · 결과 |
|---|---|---|---|
| 1 | **콜드 스타트 18.5초** (이전 라운드들은 웜 서버만 측정) | 로컬 STT(faster-whisper) 로드에 잠금이 없어 /health 폴링이 겹치면 수 초짜리 모델 로드가 **5번 동시 실행** | 로드에 잠금 — 1회만 시도 → **2.4초** (7.7×) |
| 2 | web_qa 쿼터·스트림 체크 실패 | 테스트 스텁이 실제 Anthropic과 달리 SSE data에 `type` 필드를 빠뜨림 → 파서가 토큰 0개 → **빈 스트림이 그동안 조용히 200으로 통과** (17장의 빈 응답 가드가 정확히 잡아냄) | 스텁을 실제 API 형식에 맞춤 → 16/16 복구. 가드가 옳았다는 교차 증거 |
| 3 | 공급자가 200에 JSON 아닌 본문(프록시 오류 페이지 등)을 주면 파싱 예외가 그대로 500 | 형식 오류 응답에 재시도 규율 부재 | 빈 응답과 같은 규율(백오프 1회 재시도 → 항목 스킵)로 통합 — 스텁 재현으로 확인 |

### 18.2 전 스위트 결과 (수정 반영 후)

| 검사 | 결과 |
|---|---|
| 콜드 스타트 (기동→health→첫 번역) | **2.4s / 2.9s** |
| speakability 시드 70 | 위반 0 ✅ |
| rag-eval / ooc-eval(py·ts) | 15/15 · 15/15 · 15/15 ✅ |
| web_qa 웹 계층 | 16/16 ✅ |
| E2E 34체크 ×2회 | 전부 통과 · 오류 0 ✅ |
| 백업·복원 (17장 신규 기능) | 4/4 복원 · LLM 재호출 0 · 종료 시 삭제 ✅ |
| 검색 단독/동시8 | p50 3ms / 17ms · 오류 0 ✅ |
| 제안·번역 종단 | p50 1,380ms / 454ms · 실패 0 ✅ |
| Lighthouse 모바일·데스크톱 | 전 카테고리 100 ✅ |
| 내성 3종 (버스트·429폭탄·503) | 전부 통과 · 새어나간 429 0건 ✅ |
| 15분 시뮬 (무료 한도 + 게이트웨이 기본) | 429 0건 · 80/80 · p95 435ms ✅ |
| RPD 소진 감지·복구 (맨 끝 순서로 격리) | 안내 노출 → 즉시 복구 ✅ |

**"테스트할 때마다 뭔가 나온다"에 대한 정직한 답**: 나오는 것들의 성격이
바뀌었다. 초기엔 제품 결함(검색 오류, 지연 폭증)이었고, 지금은 측정 사각지대
(콜드 스타트)와 테스트 인프라의 결함(스텁 불일치)이다. 제품 코드 자체에서
새 결함이 나오지 않는 상태에 도달했고, 남은 미지수는 실키·실음성뿐이다.


## 19. 오버레이(PiP) 모드 + 답변 길이 4단계 (2026-08-23)

**배경**: 면접은 보통 Zoom/Meet 전체화면으로 진행된다. "옆에 두는 창"은 미팅
화면에 가려져 실전에서 쓸 수 없다는 피드백 → 미팅 화면 **위에** 항상 떠 있는
작은 창이 필요했다.

### 19.1 오버레이 — Document Picture-in-Picture (app.html)
- 헤더의 🪟 버튼 → 라이브 뷰(자막·제안 카드·4버튼·퀵 번역)와 상태줄이
  **항상-위(always-on-top) 작은 창**으로 이주. Zoom 데스크톱 앱·전체화면 Meet
  위에도 뜬다 (Chrome 116+의 Document PiP — 웹에서 가능한 유일한 방식).
- 설계 원칙: **창은 '뷰'만 옮긴다.** 마이크·음성 인식·LLM 호출·백업은 전부
  본 탭에서 계속 돈다. DOM 노드를 통째로 이주시키므로 이벤트 핸들러·진행 중인
  스트림 렌더링이 끊기지 않는다. 닫으면(X 또는 🪟 재클릭) 본창으로 자동 복귀.
- 컴팩트 스타일(body.overlay): 380×560 기준 여백·폰트 1단계 축소, Safe 문장은
  여전히 크게. 창 크기는 조절 가능하고 localStorage에 기억된다.
- 폴백: PiP 미지원 브라우저는 안내 후 기존 "좁은 창 옆에 두기" 모드 유지.
- 검증: Playwright 20/20 — 이주/복귀/왕복 2회 무결, 이주 후 발화 추가·4버튼
  핸들러 생존, 380px 가로 스크롤 없음. E2E 34항목 회귀 전부 통과(콘솔 오류 0).

### 19.2 답변 길이 4단계 (backend/prompts.py ANSWER DEPTH)
실제 면접 관행 기준으로 질문 유형별 길이를 세분화:
| 유형 | 길이 | 예 |
|---|---|---|
| 스몰토크 | 1문장 | how are you |
| 사실 확인·조건 | **2문장 상한** (결론+증거 1개) | notice period, "experience with X?" |
| 실질 질문 | 3~6문장 (15~25초) | why this company |
| 행동 딥다이브 | 5~8문장 미니 STAR, **숫자로 끝** | tell me about a time |
| 간결 신호 | 1~2문장 강제 | "briefly", "in 30 seconds" |
- suggest max_tokens 700→900 (server.py): 5~8문장 × EN+KR 병기 2안 + META면
  700이 빠듯 — 상한 잘림은 형식 파손(KR 누락)으로 이어진다.
- 실키 검증: 사실 확인 2문장(SK $55.2M 증거 1개) / 실질 6문장 / 행동 8문장
  (Story B, "890 million won in one quarter"로 마무리) / 간결 1문장. 노티스
  기간 질문에서 숫자를 지어내지 않음(가드레일 유지).

## 20. 단일 답변 전환 + 발표형(1~2분) 티어 (2026-08-23, 사용자 피드백)

**피드백**: ① 답변 제안은 1가지만 ② 상황에 따라 1~2분 답변 필요 — 현재는 단조로움.

### 20.1 단일 답변 (Safe/Rich 2안 폐지)
- 프롬프트 형식: EN/KR 1블록 + META. **첫 문장 ≤8단어 = 즉답 오프너** —
  스트리밍 중 첫 문장부터 바로 읽기 시작하므로 Safe안의 "즉시성"은 유지된다.
- UI: 카드 라벨 [답변] 하나. 45단어 초과 답변은 기본 폰트+넓은 줄간격으로
  전환(.long) — 발표형에서 큰 폰트가 만드는 스크롤이 더 해롭기 때문.
- 계약 갱신: mock_gemini 단일 블록 출력, e2e "단일 답변 · 오프너≤8·이후≤12단어",
  ooc-eval judge 단일 답변 기준. E2E 전 항목 통과(콘솔 오류 0).

### 20.2 ANSWER DEPTH 5단계 (기존 4단계 + 발표형)
| 유형 | 길이 | 실키 실측 |
|---|---|---|
| 스몰토크 | 1문장 | 1문장·6단어 |
| 사실 확인 | 2문장 상한 | 3문장·10초 |
| 실질 | 4~7문장 (20~30초) | 7문장·27초 |
| 행동 딥다이브 | 8~12문장 (45~75초), 숫자로 끝 | 11문장·41초·"890M in one quarter" |
| **발표형** (자기소개·테리토리 플랜·pitch me) | **16~22문장·180~250단어 (90~120초)**, 3테마 + 구어 표지("First/Second/To wrap up") | 자기소개 17문장·147단어 / 플랜 17문장·174단어 — 자연 발화 기준 1분+ |
- "briefly/30초" 신호는 전 유형을 1~2문장으로 오버라이드 (실측 1문장).
- suggest max_tokens 900→1400 (발표형 EN+KR 병기 잘림 방지).

### 20.3 "UI가 안 바뀐 것 같다" 대응
- 원인: 오버레이는 🪟 버튼을 눌러야 나타나는 기능이라 변화가 안 보였음.
- 조치: 🪟 버튼 강조 테두리(액센트), 첫 화면 안내 문구에 오버레이 사용법,
  서비스워커 캐시 v2 범프(구 캐시 강제 교체).

## 21. UI 미반영 진단 + 45분 지속 품질 검증 (2026-08-23)

### 21.1 "UI가 안 바뀌었다" 진단
- **주원인**: 터미널 기록 분석 — 오버레이 커밋(1afddba)을 pull한 것이 해당
  세션의 마지막 동작. pull 이후 start.sh 재기동·브라우저 재접속이 없어
  화면에는 pull 이전 버전이 떠 있었다.
- **부차**: 오버레이는 🪟 버튼을 눌러야 나타나는 기능 — 누르기 전엔 변화 없음.
- **재발 방지**: 헤더 버전 칩(v2.1) + start.sh 기동 요약에 UI 버전 줄 —
  터미널과 브라우저 칩이 다르면 ⌘⇧R. 렌더 검증 800px/400px 완료.
- **부수 발견**: "키에 한글·공백" 경고 오탐 — 맥 기본 bash 3.2 + UTF-8
  로케일에서 범위 글롭([!-~])이 콜레이션을 타는 문제. LC_ALL=C tr로 교체.

### 21.2 45분 소크 (무료 한도 강제 — 최악 조건)
발화 360개 · 7.5초 간격 · 45.2분 실측: **429 0건 · 번역 240/240 표시(누락 0)
· 표시 지연 p50 429ms / p95 438ms / max 473ms · JS 오류 0** → 45분 품질 유지.
번역 RPD 소모 546/1000 — 무료 한도로도 인터뷰 1회 + 리허설 일부 가능.

### 21.3 paid 티어 레인 예산 (유료 결제 계정 반영)
- 문제: 레인 한도가 티어 무관 무료값 고정 → paid인데도 "제안 250/일"로
  계산돼 45분 후반(70% 소진) 자동 제안 자동 차단 위험.
- 수정: paid 기본값 fast 120 RPM/10000 RPD · main 60 RPM/5000 RPD
  (게이트웨이 60/120과 정합, env 재정의 유지).
- 검증: paid 5분 시뮬 — 칩 "⚡6/120 · 오늘 584/10000", 429 0건, 번역 27/27,
  자동 제안 게이팅 없이 동작(5분간 제안 31회).

### 21.4 QA 배터리 (최종 코드 기준)
E2E 전 항목 ✅ / rag-eval 15/15 ✅ / ooc-eval 15/15 ✅ / speakability 70개
위반 0 ✅ / 45분 소크 ✅ / paid 5분 ✅. 과정 중 ooc-eval 500은 테스트 구성
실수(mock 한도 미완화)로 판명 — 제품 결함 아님. 판정기 1건 갱신: "5년 후"
질문이 자기소개(커리어 방향) 자료를 인용하는 것은 무관 인용이 아니므로
C-티어 허용 목록에 추가.

## 22. 화자 분리(BlackHole) + 아이스브레이킹 반응 (2026-08-23, 리허설 피드백)

**피드백**: ① 내 발화까지 읽혀 화자 분리가 안 됨 — 상대 소리는 BlackHole로
받고 싶다 ② 아이스브레이킹 발화에 제안이 안 뜸.

### 22.1 화자 분리 — BlackHole 상대 전용 입력
- 구조: 웹 엔진은 인식된 모든 발화를 '상대'로 처리 → **입력 장치 자체를
  상대 소리의 복사본(BlackHole)으로 바꾸면** 내 목소리는 아예 유입되지 않아
  완전한 분리가 된다. 코드가 아니라 오디오 라우팅이 정답인 문제.
- 설정(1회): 다중 출력 기기(스피커+BlackHole) → 시스템 출력 / Chrome 마이크
  = BlackHole / Zoom 마이크 = 실제 마이크 유지 — FIELD-TEST §3.5에 절차 수록.
- 앱 지원: ▶ 시작 시 상태줄에 입력 장치 표시 — "🎧 상대 전용(BlackHole) —
  화자 분리 ON"(초록) / "⚠️ [장치명] — 내 목소리도 잡힘"(경고+설정 안내 툴팁).
  설정이 맞았는지 실행 즉시 눈으로 판별된다.

### 22.2 아이스브레이킹 반응 (wantsReply ICEBREAK)
- 기존 감지기는 질문·반론 신호만 봐서 "Nice to meet you", "Thanks for
  joining" 같은 인사에 침묵 — 면접 첫 1~2분이 전부 이 유형이다.
- ICEBREAK 신호 29종 추가 (인사·감사·오디오 확인·시작 전 멘트). 스몰토크
  티어(1문장)와 결합되어 짧은 리액션 제안이 뜬다.
- 검증: 감지 8/8 (인사 6종 트리거, "Sure, sounds good"류 비트리거 유지),
  마이크 표시 2상태 렌더 확인, E2E 전 항목 회귀 통과(콘솔 오류 0).

## 23. 제안 카드 하단 패널화 — 자막 비겹침 (2026-08-23, 리허설 피드백)

**피드백**: 답변 카드가 자막을 덮어 "상대가 무슨 말을 하는지 모른 채 답하게
된다" + 오버레이(🪟) 기능의 존재를 모르고 전체화면 탭으로 사용.

- **카드 = 팝업 → 하단 패널**: 카드가 자막 위에 뜨는 절대배치를 폐지하고,
  화면을 자막(위)·카드(아래)·버튼(맨 아래)이 나눠 쓰는 in-flow 패널로 전환.
  카드 최대 높이 48%(오버레이 52%), 카드가 스트리밍으로 자랄 때마다
  피드를 바닥에 고정(fitCard)해 **최신 자막이 항상 보인다.** 검증: 카드/피드/
  컨트롤 비겹침 + 피드 바닥 고정 오차 0px (1280px·380px 양쪽), E2E 통과.
- **오버레이 발견성**: ▶ 시작 시 1회 배너 "🪟 버튼: Zoom·Teams·Meet 위에
  항상 떠 있는 작은 창으로 전환됩니다" (localStorage 1회 노출).
- 버전 v2.2, SW 캐시 v3.

## 24. 오버레이 인터뷰 모드 — 단일 모니터 "이중 화면" 개선 (2026-08-23, v2.3)

**목표**: Zoom/Meet 전체화면 + PiP 오버레이만으로 인터뷰. 시선이 카메라(상단)
근처에 있어야 하므로, 작은 창에서도 답변을 즉시 읽을 수 있어야 한다.

### 구현 (app.html 단일 파일 · 서버 API 무변경)
- **인터뷰 모드 (오버레이 기본)**: PiP 안에서 상태줄·버튼·입력을 숨기고
  [답변 카드(최상단·전폭) + 최신 자막 1~2줄(하단)] 세로 스택만 표시.
  카드가 열리면 CSS order로 자동 최상단. `/` 키는 전체 모드로 전환 후
  입력창 포커스. 화자 분리 상태는 컨트롤 바의 미니 표시(🎧/⚠️)로 유지.
- **컨트롤 바** (오버레이 전용, 본창 숨김): [전체/인터뷰] [S/M/L] [70%].
- **크기 S/M/L + 폰트 자동 스케일**: vw 기반 clamp — 창을 화면 1/4(320px)로
  줄여도 답변 첫 문장 19.2px(실측) 유지, 드래그 리사이즈에도 비례.
  프리셋은 스케일 계수(0.9/1/1.15) + resizeTo 시도(Chrome이 막으면 다음
  열기부터 프리셋 크기 적용).
- **배경 70%/100%**: 표면 알파 토글. ⚠️ Document PiP 창은 OS 창이라 뒤의
  Zoom이 실제로 비치지는 않음(Chrome 제약) — 70%는 시각적 무게 감소 모드.
  텍스트 대비는 백색 합성 가정에서도 4.5:1(AA) 이상 확인.
- **설정 기억**: mode·alpha·size·창 크기를 localStorage(mc_ov, mc_pip_w/h)에
  저장, 전 접근 try/catch. 재열기·재실행 시 복원.
- 버전 v2.3 · SW 캐시 v4.

### 검증
- e2e 신규 11케이스: PiP 열림·기본 인터뷰 모드·카드 최상단/전폭/자막 1~2줄·
  프리셋 폰트 스케일·70% 토글·모드 전환·localStorage 저장·재열기 복원·
  저장 크기 재열림·본창 복귀. **전체 e2e 통과, 콘솔 오류 0.**
  (참고: 헤드리스는 PiP 폭을 뷰포트로 클램프 — 크기 검증은 저장값 기준)
- 회귀 확인: 백업/복원·⏹ 자산화·화자분리 표시·카드 비겹침 전부 통과.

## 25. 오버레이 플로팅 패널 재디자인 + 본창 폴리시 (2026-08-24, v2.4)

**계기**: 사용자가 Smooth AI 데스크톱 오버레이 스크린샷을 첨부 — "UI 전체적으로
개선 + 🪟 클릭 시 분리된 작은 창이 아니라 스크린샷 같은 UI였으면".
웹에서 항상-위 창은 Document PiP뿐이므로(별도 창이라는 사실 자체는 Chrome
제약), **PiP 창 내부를 레퍼런스와 같은 플로팅 패널로 전면 재스타일**하는
방향으로 합의된 계획을 실행.

### 구현 (app.html 단일 파일 · 서버 API 무변경)
- **플로팅 바**: ⏸(듣기 일시정지)·⏹(미팅 종료) 원형 버튼 + 밝은 "라이브" 필
  + [전체][S][M][L][70%] 미니 컨트롤 + 🎧 화자분리 표시. ⏸⏹은 본창
  btn-listen/btn-stop의 프록시 — 로직은 한 곳만 유지.
- **요약 스트립**: 상태줄을 오버레이에서 호박색 점 + "실시간 요약" 라벨의
  스트립으로 재배치(order:-1, 최상단). RAG 카운트는 오버레이에서 숨김.
- **유리(글래스) 표면**: 배경 rgba(15,17,23,var(--ovA)), 버블·카드·스트립은
  rgba(255,255,255,.05~.16) 무테두리 라운드(10~16px). EN은 600 굵기 #F2F5F9.
- **고스트 액션**: 동의/반박/질문/제안 4버튼을 투명 배경 텍스트 버튼으로,
  `.more` 보조 2버튼은 오버레이에서만 숨김. 입력창은 radius 999px + 원형
  30px ↑ 전송 버튼(레퍼런스 동일).
- **기본 반투명**: alpha 기본값 70(--ovA .82). 70%↔100% 토글은 유지하되
  기본이 반투명으로 반전 — 레퍼런스 톤.
- **인터뷰 모드 계약 변경**: v2.3의 "상태줄·버튼·입력 숨김" → v2.4는
  **표시가 기본**(요약 스트립·고스트 액션·라운드 입력 모두 노출, 스크린샷
  구성과 동일). 인터뷰↔전체의 차이는 답변 카드 위치(최상단↔하단)와 자막
  높이(1~2줄↔전체)만.
- **본창 폴리시(저위험만)**: 헤더 여백·버튼 34×30 radius9 + hover 트랜지션·
  버블 radius13. 기능 무변경.
- 버전 v2.4 · SW 캐시 v5.

### 검증
- e2e 오버레이 섹션을 v2.4 계약으로 갱신(⏸ 존재·요약 스트립/고스트 액션
  표시·`.more` 숨김·기본 ov-70 ON·토글 후 OFF·alpha=100 저장·전체 모드 =
  카드 order 1). **전체 e2e 통과, 콘솔/네트워크 오류 0.**
- 380px 시뮬 스크린샷으로 레퍼런스 구성(요약 스트립/카드/자막/고스트 액션/
  라운드 입력) 재현 확인.
- 회귀: 백업/복원·⏹ 자산화·수동 동기화·복습 SRS·카드 비겹침 전부 통과.

### 수동 체크리스트 (맥북)
1. `git pull` → `bash start.sh` → 크롬 ⌘⇧R → 헤더 배지 **v2.4** 확인
2. ▶ 시작 → 🪟 클릭 → PiP 안에 [⏸⏹ 라이브 바 / 호박색 요약 스트립 /
   답변 카드 / 자막 버블 / 동의·반박·질문·제안 / 라운드 입력] 확인
3. ⏸ 눌러 본창 듣기 토글 연동, ⏹ 눌러 종료 요약 동작 확인
4. 70% 버튼으로 불투명 전환, S/M/L 크기, 창 닫고 재열기 시 설정 복원

## 26. 글랜스 UI — 인지 부하 최소화 (2026-08-24, v2.5, 표시 계층만)

**전제**: 미팅 중 0.5초 흘끗 보기로 추천 문장이 읽혀야 한다. 백엔드·API·
상태 관리·스트리밍 로직은 무변경 — 렌더링·CSS·표시 전용 상태만 수정.

### 진단 (수정 전, 우선순위순)
D1 답변 16.5px가 주변 장식과 무게 경쟁 · D2 생성 시작 시 이전 답변 즉시 삭제
· D3 자막 무조건 바닥 고정 스크롤 · D4 화자 구분이 색상 미세 차이뿐 ·
D5 카드 개봉 시에도 주변 UI 동일 톤 + 근거 블록 3줄.

### 구현
- **추천 카드(D1·D5)**: primary EN 19px semibold(글자 크기 배율과 곱연산),
  발표형(>45단어)만 14.5px 예외 유지(큰 폰트=스크롤=글랜스 저해).
  답변은 CSS로 한 화면 최대 2개(nth-of-type(n+3) 숨김). 새 답변 도착 시
  배경 하이라이트 300ms 페이드(ansIn). 📚 배지 아웃라인화.
  📎 근거는 12px 회색 **한 줄**(ellipsis).
- **회색조(D5)**: `:has(#card[style*="block"])`로 카드 개봉 동안 피드·상태줄·
  헤더에 grayscale(.5) brightness(.86) — 시선을 카드로.
- **트랜스크립트(D3·D4)**: 스크롤 앵커 — 바닥 72px 밖으로 스크롤하면 자동
  스크롤 해제 + "새 메시지 ↓" 필 버튼(0-height 래퍼로 피드 위 부유),
  클릭·바닥 복귀 시 재앵커. fitCard도 앵커 존중. 화자 구분은 정렬로
  (상대 좌/나 우, 버블 max-width 88%) — me 배경색 차이는 제거.
- **레이턴시 체감(D2)**: 생성 시작 시 이전 답변을 지우지 않고 .gen으로
  45% 투명 유지 + 카드 헤드 하단 2px 진행 바. 3초+ 지연 시 "생성 중…"
  텍스트(.slow). 첫 토큰에 해제. abort 경합 가드(현재 요청만 정리),
  게이트웨이 드롭 시 이전 요지·답변 복원. 기존 로딩/에러 상태값 소비만.
- 버전 v2.5 · SW 캐시 v6.

### 검증
- e2e 신규 5케이스(≥18px semibold·정렬·앵커 해제+버튼·복귀·생성 중 유지)
  + 기존 대기 조건을 v2.5 계약(.gen 해제+근거 표시)으로 갱신.
  **전체 e2e 통과, 콘솔/네트워크 오류 0.**
- 375px 자가 검증 스크린샷 2회 — 1차에서 📚 배지가 시선을 뺏는 문제를
  발견해 아웃라인으로 수정 후 재확인.
- 백엔드 변경 없이 불가능해 스킵한 항목: 없음 (RAG 근거는 이미 프론트로
  내려오고 있어 재스타일로 충족).

## 27. 카드 정렬 회귀 수정 + 사람다운 답변 톤 (2026-08-24, v2.6, 실전 피드백)

**피드백**: ① 넓은 화면에서 답변 카드만 왼쪽 구석에 붙음(가독성 저하) —
"굳이 왼쪽에 둔 이유가 있나?" ② 답변이 너무 AI스러움(수치 5개 연쇄 나열 +
"First/Second" 골격) — "그러지 말라고 RAG를 구축한 것".

### ① 카드 왼쪽 붙음 — 의도 아님, v2.2 회귀
넓은 화면 중앙 정렬 규칙(min-width 760px, margin:auto)을 v2.2에서 카드를
하단 패널로 바꿀 때 추가된 `#card{margin:0 8px 6px}`(더 뒤에 선언)가
덮어써서, 피드·버튼·입력은 660px 컬럼 중앙인데 카드만 왼쪽에 남았다.
→ 카드 전용 미디어 규칙을 뒤에 추가해 같은 컬럼으로 복귀. 1600px 스크린샷
검증. (모바일 375px에는 영향 없음)

### ② SOUND HUMAN 규칙 (backend/prompts.py)
"이력서 낭독" 3원인을 규칙화:
- 수치 하드캡: 답변당 최대 2개(발표형만 3), 연쇄 나열 금지
  ("38 accounts, 26.8→50.7M, 89%" 패턴 명시 금지). 이름도 답변당 1곳.
- 수치는 말하듯: "about fifty million", "we almost doubled it" —
  스펙시트 화법("that's an 89 percent growth") 금지. 정확값은 물을 때만.
- "First/Second/Third" 번호 골격 금지 → 자연 전환("So a bit about...",
  "The other thing is...", "And looking ahead..."). 발표형 3테마는 유지하되
  테마당 앵커 1개(숫자든 고객사든 스토리든)로 제한.
- 템플릿 마무리("This taught me...") 금지, 가벼운 리드는 답변당 1회까지.

### 검증
- 실키 샘플("tell me about yourself, take your time"): 수치 3개(상한 준수)·
  "about fifty million" 반올림 화법·번호 골격 없음·고객사명 1곳 — 통과.
- 전체 e2e 통과(콘솔 오류 0), speakability 문장 상한 규칙과 충돌 없음.
- 버전 v2.6 · SW 캐시 v7.

## 28. UI 감사 마감 — P1~P3 폴리시 (2026-08-24, v2.7)

"수정할 부분이 전혀 없나?" 질문에 상태별 캡처 감사로 답한 라운드.
발견 3건 전부 CSS만으로 해결, 로직 무변경.

- **P1 헤더 2줄 랩(900~1400px)**: 헤더가 660px 컬럼 패딩에 갇혀 중간 폭에서
  셀렉트가 고아 줄로 꺾였다 → 헤더만 최대 1100px 존을 중앙 정렬
  (`max(16px, calc((100vw-1100px)/2))`). 900px 실측 높이 49px(1줄).
  상태줄은 기존 660px 컬럼 유지.
- **P2 발표형 답변 잘림**: 카드 max-height 48%에서 18문장 중 ~8문장만 보여
  읽는 도중 스크롤 필요 → `#card:has(.lrow.long)`만 65%로 확대.
  375px 실측: 카드 463/713px=65%, 자막 잔여 116px(1~2줄 유지), 12문장 노출.
  오버레이 규칙이 뒤에 선언되어 오버레이 동작 무영향.
- **P3 생성 중 제목 랩**: 질문 원문이 요지 자리에 들어와 2줄로 꺾이며
  "생성 중…"을 밀어냄 → gist 1줄 고정(min-width:0 + nowrap + ellipsis).
  실측 18px(1줄).
- 감사에서 수정 비권장으로 남긴 것: 첫 실행 빈 화면 문구 위치(3초 화면),
  데스크톱 중간 여백(좁은 패널/오버레이가 주 사용 형태 — 의도된 레이아웃).
- 전체 e2e 통과(콘솔 오류 0). 버전 v2.7 · SW 캐시 v8.

## 29. 보류 2건 마감 — 하단 정렬 자막 + 3단계 온보딩 (2026-08-24, v2.8)

§28에서 "수정 비권장"으로 남겼던 2건을 사용자 요청으로 진행. CSS + 안내
마크업만 변경, 로직 무변경.

- **데스크톱 중간 여백**: 자막이 위에서부터 쌓여 [자막 몇 줄 / 거대한 여백 /
  카드]로 시선이 찢어졌다 → 자막을 채팅처럼 **아래에서 위로** 쌓는다
  (`#feed` flex column + `> :first-child{margin-top:auto}` 트릭 — 내용이
  넘치면 auto가 0이 되어 일반 스크롤로 자연 전환). 최신 자막이 항상 카드·
  입력 바로 위에 있어 시선 동선이 하단에 모인다. 1280px 실측: 마지막 자막
  ↔ 피드 바닥 20px.
- **첫 실행 안내**: 상단 1/3의 4줄 문단 → 세로 중앙(실측 위/아래 167px 균등)
  의 **3단계 카드**(① ▶ 듣기 → ② 근거 답변 카드 → ③ 🪟 오버레이) —
  읽는 순서가 곧 사용 순서.
- 전체 e2e 통과(콘솔 오류 0). 버전 v2.8 · SW 캐시 v9.

## 30. 응답 속도 개선 — 프롬프트 다이어트 · quick fast레인 · 투기 강화 (2026-08-24, v2.9)

**피드백**: "추천 답변셋 응답 속도가 느려졌다."
**진단**: (주의) 원격 검증 환경은 프록시가 SSE를 버퍼링해 절대 TTFT가 왜곡됨
— 상대 비교와 구조 요인으로 판단. 제어 가능한 요인 3개:
규칙 누적으로 프롬프트 ~6,211자(≈1,550토큰), 1~2문장 인텐트까지 main
레인(flash), 투기 생성이 무료 티어 기준 보수값.

### 구현
- **프롬프트 다이어트 (prompts.py)**: 규범(문장 상한·티어 범위·수치 캡·금지
  목록)은 전부 유지, 서술만 압축 + RAG 인용 500→350자/건.
  **6,211 → 4,754자 (-23%)**.
- **quick fast레인 (server.py)**: buytime(시간 벌기)·clarify(되묻기)는 정의상
  1~2문장 → fast 레인(lite 모델)·max_tokens 400. 실측 **총 1.0~1.2초**.
  클라이언트 로컬 쿼터 카운트도 fast로 정합(app.html).
- **투기(미리) 강화 (app.html)**: paid 티어면 질문당 최대 2→4발,
  발동 지연 600/500~1500ms → 450/350~1200ms. 무료 티어는 기존값 유지 —
  질문이 끝나는 순간 답이 이미 떠 있는 것이 최고의 체감 속도.

### 검증 (실키)
- 시간벌기 1.23s · 되묻기 1.03s · 사실확인 1.13s (총 소요, 2문장 계약 준수)
- 발표형 3회: TTFT 1.4~3.9s · 총 3.3~5.8s · 문장 16~17(계약 준수) —
  다이어트 전 대비 TTFT 개선 경향, 품질 계약(번호골격 0·수치 캡) 유지
- 전체 e2e 통과(콘솔 오류 0; 번역 표시 1회 플레이크는 재실행 통과 — 기지의
  부하성). 버전 v2.9 · SW 캐시 v10.

## 31. 시선·발음·연속 질문 — 실전 요구 3건 (2026-08-24, v3.0)

**요구**: ① 카메라가 화면 위 — 읽을 때 눈동자가 위를 향하게 배치
② 답변에 발음기호·한국어 발음 병기 ③ 질문에 바로 이어지는 질문에 무반응.

### ① 인터뷰 프리셋 카드 상단 배치 (app.html CSS 1줄)
🎤 인터뷰 프리셋의 본창에서 답변 카드를 `order:-1`로 화면 최상단에 —
읽는 동안 시선이 카메라 근처. 자막은 아래(하단 정렬 유지). 오버레이는
기존 인터뷰 모드가 이미 카드 상단이라 그대로.

### ② PR(발음) 라인 (prompts.py·app.html·server.py)
형식에 `PR:` 추가 — EN 전체를 **한글 발음**으로, 어려운 단어(3음절+/도메인
용어) 뒤에만 **/IPA/** 병기. 실키 검증: "워크플로/ˈwɜːrkfloʊ/
오토메이션/ˌɔːtəˈmeɪʃən/…" 형태 확인. UI는 KR 아래 🗣 줄(한 톤 낮게).
출력 상한 1400→2400(quick 400→700) — EN이 먼저 스트리밍되므로 읽기 시작
시점은 그대로. mock에도 PR 추가, e2e 케이스 1건 신설.

### ③ 연속 질문 무반응 (app.html)
원인: 새 질문의 투기/자동 제안이 게이트웨이에서 드롭되면(연속 질문 =
겹침 부하) **조용히** 사라지고, v2.6부터는 이전 답변 복원까지 하므로
"반응 없음"으로 보였다. 수정: 드롭 시 500ms 뒤 일반 자동 제안으로 **딱
1회 재발사**(그 사이 새 요청이 시작됐으면 양보), 투기 상태 해제로 최종
발화의 중복 억제 해소. 프롬프트에도 "한 턴에 질문이 여러 개면 마지막
질문에 답하라(앞 질문은 한 구절로만)" 규칙 추가 — 실키 검증: 역할+노티스
연속 질문에서 앞을 짚고 마지막에 답함.

### 검증
- 실키: PR 한글+IPA 형태, 연속 질문 처리, 계약(오프너 ≤8단어) 유지
- 375px 스크린샷: 인터뷰 프리셋에서 카드 최상단 + PR 줄 표시
- 전체 e2e 통과(콘솔 오류 0). 버전 v3.0 · SW 캐시 v11.

## 32. 지시어 맥락 해소 — 답변 주제 오판 수정 (2026-08-25, v3.1, 리허설 피드백)

**사례**: "could you study about our company" 직후 "what are your strengths
and weaknesses" → 자막 번역은 "저희의 강점과 약점"으로 맥락을 잡았는데,
답변은 후보자 본인의 강약점을 말함 — 주제 오판.
**원인**: 번역 프롬프트와 달리 답변 프롬프트에는 지시어(your/our/that/it)를
맥락으로 해소하라는 규칙이 없어, 직전 턴이 세운 주제 프레임을 무시하고
문자 그대로 읽음.

### 수정 (prompts.py 규칙 1블록)
RESOLVE REFERENTS FROM CONTEXT — 지시어는 최근 맥락(직전 턴 최우선)이
가리키는 대상으로 해석. 해석을 오프너에 드러내("From my research on your
platform, ...") 오판이어도 화자가 즉석에서 바로잡기 쉽게. 맥락 단서가
없으면 평문 그대로.

### 검증 (실키, 문제 시나리오 재현)
- 맥락 있음: "From my research on Workato, here's my take. Your biggest
  strength is product leadership. You've been a Gartner iPaaS Leader for
  seven years straight…" — 회사의 강약점으로 정답. "7년 연속 Leader"는
  코퍼스(Workato 회사·재무 팩트)에 실재 — 그라운딩 확인.
- 맥락 없음: 본인 강약점으로 답변 — 평문 해석 회귀 없음.
- 전체 e2e 통과(콘솔 오류 0). 버전 v3.1 · SW 캐시 v12.

## 33. 실전 전 전수 자가 진단 (2026-08-25, v3.1 기준)

8/27 투입 전 최종 점검 — 원격 환경에서 검증 가능한 전 항목 실행.

| 항목 | 결과 |
|---|---|
| e2e 전체 (UI·오버레이·PR·복습·동기화) | ✅ 전부 통과, 콘솔 오류 0 |
| rag-eval (검색 15쿼리) | ✅ 15/15 |
| ooc-eval + speakability (시드 밖 15문항) | ✅ 15/15 (.py·.ts 모두) |
| resilience-sim (버스트·429폭탄·503) | ✅ 전부 통과 — 새어나간 429 0, JS 오류 0 |
| rpm-sim 5분 (paid 예산) | ✅ 429 0건 · 번역 27/27 · p95 450ms · JS 오류 0 |
| 실키 스팟체크 | ✅ 발표형 17문장·오프너 8단어·PR·번호골격 없음 / 사실확인 2문장 1.8s / 시간벌기 1.4s |
| 개인 데이터 커밋 여부 | ✅ store.db·secret.key·imported/ 커밋 없음, 실키 하드코딩 없음 |
| 쿠팡·크래프톤 정책 | ✅ 커밋 시드에 없음, 로컬 청크도 이름만(수치 0건) |
| 버전 정합 | ✅ 칩 v3.1 · SW v12 · start.sh 자동 추출 |

**진단이 잡은 결함 1건(수정 완료)**: tests/ooc-eval.ts가 v2.0 '단일 답변'
전환 때 갱신 누락된 구 계약(2안 Safe/Rich·≤9단어)으로 남아 0/15 오탐 —
현행 계약(ooc_eval.py와 동일)으로 동기화 후 15/15.

**원격에서 검증 불가 — 맥북 리허설에서 확인할 것**: BlackHole 라우팅
(🎧 상대 전용 표시), Chrome PiP 오버레이 실동작, 실제 체감 TTFT(프록시
없는 환경), 마이크 권한, 실전 설정(프리셋 🎤 인터뷰 + 모드 🔒 실미팅),
맥 코퍼스(Workato 44청크 포함 184청크) 적재 상태.

## 34. 대화 전환 인지 — 중간 질문 감지 + 낡은 답변 표시 (2026-08-25, v3.2)

**피드백**: "다음 대화로 넘어갈 때 인지 못하고 이전 질문의 답변셋이 남는다."
**원인 2갈래**:
① STT는 물음표를 주지 않는데 wantsReply가 문두 의문사만 검사 —
  "my next question is what...", "moving on, could you..." 류의 **문장 중간
  질문**을 놓쳐 자동 제안이 아예 안 뜸.
② 새 발화가 와도 카드의 이전 답변에 "이전 것"이라는 표시가 없어 새 질문의
  답으로 오독됨.

### 수정 (app.html)
- **Q_LEADS 포함-검사**: 'question is / my next question / i'd like to ask /
  let me ask / wondering about / moving on to / let's talk about / tell me
  about / walk me through' 등 19패턴 — 문장 어디에 있어도 질문으로 감지.
- **쉼표도 문장 경계로**: "moving on, could you..."의 후행 절 검사.
  (비질문 절 오탐 → 불필요 자동 제안 가능성은 paid 예산에서 수용 —
  침묵보다 과반응이 낫다는 판단)
- **stale 표시**: 새 상대 발화가 왔는데 새 생성이 시작되지 않으면 카드에
  호박색 라벨 "⤴ 이전 발화의 답변 — 새 질문이면 아래 버튼을 누르세요" +
  답변 55% 흐림. 새 생성 시작 시 해제. 📌 고정 중엔 표시 안 함(의도적 유지).
- .gen 중에도 ":has(.kr)" 라벨 "↻ 새 답변 생성 중 — 아래는 직전 답변".

### 검증
- e2e 신규 3케이스: 중간 질문 감지(무물음표 2형 + 맞장구 비오탐)·stale
  표시·생성 시작 시 해제 — **전체 e2e 통과, 콘솔 오류 0.**
- 375px 스크린샷으로 stale 라벨 렌더 확인. 버전 v3.2 · SW 캐시 v13.

## 35. 답변 품질 3종 — 사실 정정·발표형 단축·발음 표기 규칙 (2026-08-25, v3.3)

**피드백**: ① 자기소개가 조금 긺 ② "AI 에이전트 성숙도 프레임워크를
개발했다"는 거짓 — 실제는 레벨별 성숙도에 따른 제안 방법을 알고 적용하는
것 ③ 한국어 발음 표기 오류 (honestly→아너슬리, SoW 음절 처리 등).

### ① 코퍼스 사실 정정 (근본 원인 — 데이터)
생성이 지어낸 게 아니라 **코퍼스의 개인 노트 3청크가 저작·채택을 주장**
(내 프로필 "Author of...adopted as unit-wide GTM", L1-L4 본체 "2026.07
발표·유닛 채택·키노트", 출처 검증 대응 "실행 프레임을 만들었습니다...
구조화했고...채택돼") → 그라운딩 규칙이 충실히 인용한 것.
**tools/fix_claims.py** 신규(멱등) — 위 주장을 "활용/적용" 사실로 정정하고
검색 인덱스(postings)까지 재구축. 원격 DB 정정 3건·잔존 0건 확인.
⚠️ 맥북 DB도 로컬이므로 `python3 tools/fix_claims.py` 1회 실행 필요.
프롬프트에도 가드 추가: "자료가 USE/APPLY라고 하면 built/authored로
승격 금지."

### ② 발표형 길이: 16-22문장(90-120초) → 12-16문장(140-190단어, 60-90초)
"take your time"이면 상한(16문장) 사용. 실측: 자기소개 13문장·123단어.

### ③ PR(발음) 정확도 규칙
- 자음·음절 탈락 금지: honestly → 어니스틀리 (아너슬리 ✗), strengths →
  스트렝쓰스, asked → 애스크트
- 약어는 한글 자모 이름으로, IPA 없이: AWS → 에이더블유에스, SoW →
  에스오더블유, EDP → 이디피
- 숫자는 말하는 영어 그대로: 75.6 million → 세븐티 파이브 포인트 식스 밀리언
실측: "이그젝큐티브/ɪɡˈzɛkjətɪv/·컨시스턴틀리·에이티 나인 퍼센트" 확인.

### 검증
실키 자기소개 재생성(13문장·저작 주장 0·PR 규칙 준수) + 전체 e2e 통과
(콘솔 오류 0). 버전 v3.3 · SW 캐시 v14.

## 36. EN 인라인 IPA · 발음 정합 · 🎦 배경 비추기 (2026-08-25, v3.4)

**요구**: ① 한글 발음 여전히 부정확 ② 영어 본문에도 /발음기호/ 병기
③ 줌을 켜놔도 상대 얼굴이 보이게 오버레이 투명화.

### ① ② 발음 — EN 인라인 IPA + 한글은 IPA에서 유도
- EN 형식: 어려운 단어(3음절+/도메인) 바로 뒤에 /IPA/ 인라인. UI는 .ipa
  스팬(본문의 0.6배, 옅게)으로 렌더 — 읽기 흐름 유지. TTS(🔊)·📚 배지
  매칭은 IPA 제거본 사용.
- PR 규칙 강화: **한글은 철자가 아니라 자신이 쓴 IPA에서 유도** + 오답
  예시 추가(maturity → 머추리티, 머튜리티 ✗ / usually → 유주얼리 /
  executive → 이그제큐티브).
- 실키 검증: EN 5개 단어 IPA 병기, PR 한글이 IPA와 정합, IPA 제외 문장
  검사 오프너 6단어(계약 유지).
- 한계 고지: LLM 한글 표기는 근사치 — IPA가 이제 EN·PR 양쪽에 있으므로
  확신 없는 단어는 IPA를 우선 신뢰.

### ③ 🎦 배경 비추기 — "투명"의 실제 가능한 구현
Chrome Document PiP 창의 진짜 투명은 불가(OS 창 제약, §24부터 문서화).
대신 **getDisplayMedia로 줌·미트 창을 캡처해 패널 뒤에 비춘다**:
- ov-bar 🎦 토글 → 공유 선택창에서 Zoom 창 선택 → #bg-mirror 비디오가
  z-index:-1 전면 배치(brightness .72), 표면들은 어두운 스크림으로 강화.
- 로컬 렌더링만 — 스트림은 어디에도 전송·저장되지 않음. 오버레이 닫힘/
  공유 중단 시 자동 정리(stopBgMirror).
- 사용자 제스처 문서(PiP)의 navigator를 사용 — 본창 것은 활성화 부재로 거부.

### 검증
e2e 신규(EN IPA 스팬 렌더·🎦 버튼 존재) 포함 전체 통과, 콘솔 오류 0.
캡처 실동작은 헤드리스 불가 — 맥 리허설 체크리스트에 추가(FIELD-TEST).
버전 v3.4 · SW 캐시 v15.

## 37. 오버레이 제목 겹침 수정 + 체감 지연 보완 (2026-08-25, v3.5, 실전 스크린샷)

**증상**: ① 오버레이 카드에서 답변 텍스트가 제목("이직(퇴사) 사유 확인")
위로 겹쳐 보임 ② 답변이 ~6초 뒤에 표시됨.

### ① 제목 겹침 — v2.4 유리 디자인의 회귀
`body.overlay #card .head{background:transparent}` — 스티키 제목 줄이
투명이라 카드를 스크롤하면 본문이 제목 밑을 그대로 통과. →
`rgba(24,27,34,.94) + backdrop-filter:blur(8px)` 스크림(🎦 배경 비추기
중에는 더 어둡게). 사용자 상태 재현(--ansz 1.4·L 스케일·스크롤 120px)
스크린샷으로 제목 가독 확인.

### ② 체감 지연 보완 (표시까지 ~6초)
구성 요소: STT 확정 지연(발화 종료 후 1~2s, 제어 불가) + TTFT(1.5~4s) +
투기 미적중 시 재생성. 제어 가능한 두 지점을 조정:
- **투기 재사용 문턱 0.92 → 0.88**: 어미 몇 단어 차이로 선생성을 버리고
  재생성하던 것을 줄여 "질문 끝나면 이미 떠 있음" 빈도 상승.
- **드롭 재발사 500 → 350ms**.
잔여 지연이 계속되면 실측 필요: 맥에서 `bash incident.sh` — 호출별
지연(api-trace) 블록으로 STT/TTFT/게이트웨이 어느 구간인지 특정 가능.

전체 e2e 통과(콘솔 오류 0). 버전 v3.5 · SW 캐시 v16.

## 38. PR 한글 전용 + 체감 속도 추가 보완 (2026-08-25, v3.6, 품질 최우선)

**요구**: IPA는 EN에만(/…/), 한글 발음 줄에는 IPA 불요 + 답변 더 빠르게 —
단 품질 최우선.

### 안전성 선확인
투기 재사용 문턱 0.88 완화(§37)가 부분 질문 답변을 재사용할 위험 검토 —
covers()는 "최종 발화 단어 중 투기 문장 포함 비율"이라 접두(미완) 투기는
비율이 낮아 재사용 불가. 품질 안전 확인 후 진행.

### 구현
- **PR 한글 전용**: IPA는 EN 인라인에만, PR은 순수 한글(정확도 규칙은
  "EN에 쓴 IPA에서 유도" 유지). 중복 제거로 출력 ~10-15% 단축 = 스트림
  완료도 빨라짐. mock·e2e 계약 동기화.
- **투기 확대(품질 무손실)**: paid 질문당 4→6발, 초기 발동 450→350ms,
  최소 어절 6→5 — 선생성 커버리지 상승.
- 실측: 실질 TTFT 1.95s/총 3.18s · 발표형 TTFT 3.65s/총 6.61s, EN IPA
  3~5개·PR IPA 0. (프록시 환경 수치 — 맥은 더 빠름)

전체 e2e 통과(콘솔 오류 0). 버전 v3.6 · SW 캐시 v17.
잔여 지연의 남은 지배 요인은 STT 확정(1~2s)과 모델 TTFT — 추가 단축은
품질(모델 등급·프롬프트 근거) 희생 없이는 불가하다고 판단. 실측 필요 시
맥에서 bash incident.sh.

## 39. 끊어 읽기 슬래시 + ⚡1초 오프너 (2026-08-25, v3.7)

**정정된 요구**: 발음기호(IPA)가 아니라 **끊어 읽기 " / "** 를 원했던 것 —
IPA 전면 제거. + 답변 표시 목표 1초.

### ① EN 끊어 읽기 슬래시
- EN: 절 경계·3~6단어마다 " / " 삽입(호흡 위치). UI는 .brk 스팬(옅은
  강조색)으로 렌더, TTS는 쉼표로 치환(자연스러운 포즈), 배지 매칭은
  슬래시 제거본. PR은 한글만(슬래시·IPA 없음).
- 실측: EN 슬래시 13개·IPA 0·PR 깨끗. IPA 제거로 출력도 추가 단축.

### ② ⚡1초 오프너 — 2단 응답
정직한 전제: 전체 답변을 1초에 생성하는 LLM은 없다(TTFT만 1.5~4s).
달성 가능한 설계: **suggest와 동시에 fast 레인으로 '지금 말할 첫 문장
(≤8단어)'만 병렬 요청**(RAG 없는 초소형 프롬프트·max 40토큰) → 먼저
도착하면 즉시 표시(.opened=흐림 해제), 본답변 첫 토큰이 오면 renderCard가
자연 교체. 품질은 본답변(상위 모델·RAG)이 최종 — 무손실.
- 투기(이미 선생성)·퀵 번역엔 미적용. 오프너는 본답변과 같은 abort 신호
  공유(새 요청 시 함께 취소).
- 브라우저 실측(프록시 환경): 클릭→첫 문장 2.8s, 오프너 승리 후 본답변
  교체 확인. 프록시 왜곡(~1s+) 제외 시 맥 예상 ~1초 전후. 투기 적중 시 0초.
- 계층 정리: 투기 적중(0s) > 오프너(~1s) > 본답변 TTFT(1.5~4s).

### 검증
e2e 계약 갱신(.brk 렌더·오프너 공존) 포함 전체 통과, 콘솔 오류 0.
버전 v3.7 · SW 캐시 v18.

## 40. 응답 파일업 근본 수정 — "더 느려졌다"의 진짜 원인 (2026-08-25, v3.8)

**증상**: v3.6~3.7 이후 답변 생성이 오히려 느려짐(최대 8초).
**진단 방법**: 추측 대신 재현 실험 — 중단된 투기(좀비) 2개 + 오프너 동시
상태에서 본답변 TTFT를 측정.

### 원인 (실증)
- 게이트웨이 동시 상한(GW_CONC)=2는 **레인 구분 없는 전역**이고, 스트림이
  흐르는 동안 슬롯을 점유한다.
- 클라이언트가 중단(abort)한 투기 요청은 서버가 **다음 청크 write 실패
  시점까지 슬롯을 문다**(좀비). Gemini는 첫 청크까지 1.5~4초 블록.
- v3.6~3.7에서 질문당 [투기 최대 6발 + 오프너 + 본답변]으로 요청이 늘자,
  좀비+오프너가 슬롯 2개를 채워 **본답변이 뒤에서 줄을 섰다**.
- 재현 실측: 단독 TTFT 2.21s → 좀비2+오프너 4.23s (약 2배). 실전 8초 설명.

### 수정 3종
1. **GW_CONC 티어 인지**: paid 6 / free 2 (기본값). 좀비는 블록된 읽기라
   취소로 회수 불가 — 상한을 올려 본답변이 절대 줄 서지 않게 하는 것이
   근본 해법. RPM은 토큰버킷이 별도로 지켜 429와 무관(소크로 확인).
2. **오프너 bg 강등**: 본답변보다 우선순위를 낮춰 절대 앞지르지 못하게 +
   슬롯 부족 시 드롭 허용(본답변이 곧 온다).
3. **낡은 스트림 능동 취소 + 재투기 히스테리시스**: 새 본답변 시작 시 이전
   suggest 스트림은 다음 청크에서 자진 종료(중간 스트림 좀비 수명 단축),
   재투기는 새 어절 ≥4일 때만(좀비 생성 자체 감소·RPD 절약).

### 결과 (동일 재현, 더 심한 부하)
- 좀비 3 + 오프너: 본답변 TTFT **4.2s → 1.80s** (단독 2.05s와 동급 —
  파일업 소멸)
- 회귀: e2e 전체·resilience(버스트/429/503)·3분 소크 429 0건 전부 통과
  (연속 실행 시 1회 부하성 플레이크, 단독 재실행 통과). 콘솔 오류 0.
- 버전 v3.8 · SW 캐시 v19.

## 41. 레인별 슬롯 분리 — 번역·답변 균형 (2026-08-25, v3.9)

**증상**: v3.8로 답변은 빨라졌는데 실시간 번역(자막 KR 줄)이 느려짐.
**원인 (구조)**: v3.8의 동시 6은 **전역 상한**이라 제안 계열(투기·본답변·
좀비)이 슬롯 전부를 점유 가능 — 우선순위는 대기줄 순서만 정하고 진행 중
스트림은 못 밀어내므로, 번역이 흐르는 스트림 뒤에서 굶었다. 답변을
살리려던 v3.8이 번역 쪽 균형을 깬 것.

### 수정: 동시 슬롯을 레인별로 물리 분할
- paid 6 → **fast(번역·quick·오프너) 2 + main(본답변·투기) 4**
  (free 2 → 1+1). 환경변수 GW_CONC_FAST로 재정의 가능.
- _my_turn이 토큰+슬롯 둘 다 검사, 레인이 막힌 앞사람은 건너뛴다
  (head-of-line 차단 방지 유지). release도 레인별 반환.
- 어느 쪽이 폭주해도 상대 레인의 슬롯은 침범 불가 — 균형이 구조로 보장.

### 실증 (실키)
| 시나리오 | 결과 |
|---|---|
| 번역 단독 | 1.59s |
| **제안 5개 폭주 중 번역** | **1.02s** (굶주림 소멸) |
| 좀비3+오프너 중 본답변 | 1.96s (v3.8 1.80s 동급 — 답변 유지) |

회귀: e2e·resilience·3분 소크(429 0·번역 16/16·p95 452ms) 전부 통과,
콘솔 오류 0. 버전 v3.9 · SW 캐시 v20.

## 42. 번역 놓침 근절 — 자동 재시도·빈 응답 격리·스윕 워치독 (2026-08-25, v4.0)

**증상**: 시간이 지나면 실시간 번역이 간간히 놓임(자막 KR 줄 미채움).
**원인 (코드 정독으로 3개 구멍 확정)**:
1. **일시 오류 = 영구 실패**: 간헐 5xx·순단·재시도 소진 시 "번역 실패"로
   확정하고 끝 — 재시도는 "같은 문장이 다시 나올 때"뿐인데 실전 대화에서
   같은 문장은 다시 나오지 않는다. 45분 중 몇 번의 순단 = 놓침과 일치.
2. **배치 줄 누락**: 3줄 요청에 모델이 2줄만 주면 남은 버블 영구 방치.
3. **빈 응답 캐시 오염**: 단건 번역이 0자로 끝나면 빈 문자열이 성공으로
   캐시돼 같은 문장이 이후에도 조용히 빈 채로.

### 수정 (app.html 번역 큐)
- **자동 재시도**: 모든 실패 경로(5xx·줄 누락·빈 응답)를 1.5s→4s 백오프로
  최대 2회 조용히 재큐잉('···' 유지). 소진 후에만 실패 표시. RPD 소진 중엔
  재시도하지 않음(보내봐야 429).
- **빈 응답 격리**: 0자 결과를 성공 캐시 대신 실패로 돌려 재시도 경로 태움.
- **스윕 워치독**: 10초 주기로 '···'가 12초+ 방치된 버블을 스캔해 재큐잉 —
  알 수 없는 구멍까지 막는 최후 안전망(멱등, PiP 문서 포함).

### 검증
- e2e 신규: mock 503 p=1 주입 → 발화 → 주입 해제 → **KR 자동 회복** 확인
  (주입 창의 의도된 5xx는 오류 집계에서 제외 처리).
- 전체 e2e 통과, 콘솔/네트워크 오류 0, exit 0.
- 버전 v4.0 · SW 캐시 v21.

## 43. 오버레이 가독성 — 실사용 피드백 (2026-08-25, v4.1)

**피드백**: 실제 미팅에서 오버레이(🎦 배경 비추기 포함)를 써 보니 읽기 불편.

### 수정 (CSS만)
- **텍스트 대비 상향**: 반투명 표면 위에서 묻히던 보조 텍스트를 한 톤씩
  밝게 — KR #C7CFDA, 발음(PR) #98A3B3, 메타 #8E99A8. 긴 단어 줄바꿈 허용
  (overflow-wrap).
- **자막 크기 상향**: 오버레이 버블 EN 12.5→13.5px(최대 18), KR 11→12px
  (최대 16) — vw 스케일·프리셋 배율은 그대로.
- **🎦 배경 비추기 가독성**: 영상 밝기 .72→.6, 텍스트 표면(요약 스트립·
  버블·카드·입력)은 rgba(11,13,18,.82~.86)+블러 스크림으로 영상 움직임을
  차단 — 표면 사이 틈으로만 얼굴이 비친다. 고스트 버튼은 밝은 글자+섀도.
- 검증: 최악 조건(밝고 복잡한 배경) 시뮬 스크린샷으로 전 요소 판독 확인,
  전체 e2e 통과(콘솔 오류 0). 버전 v4.1 · SW 캐시 v22.

## 44. 최종 전수 QA — 결함 사냥 + 선제 개선 (2026-08-25, v4.2)

실전(8/27) 전 최대 공수 감사. 발견·조치 전량:

### 발견한 결함 (4건, 전부 수정)
1. **진단 명령 경로 오안내**: incident.sh는 앱 루트에 있는데 문서·안내가
   `tools/incident.sh`로 잘못 기재 → 정정 (`bash incident.sh`).
2. **복원 미번역 영구 공백**: 번역 도착 전 새로고침된 발화는 복원 후에도
   KR이 영영 비어 있었다 → 복원 시 라이브와 같은 정책으로 재번역 큐잉.
3. **오프너 중복 호출**: 자체가 fast 1초대인 시간벌기·되묻기에도 오프너가
   나가 호출을 낭비 → 제외.
4. **resilience 시간 가정 낡음**: "20초 내 번역 재개" 단언이 서킷 브레이커
   (30초, 의도된 보호)와 충돌 — 동시 6에서 발동 확률 상승으로 ~50% 오탐
   → 45초 창으로 정정 (제품 동작은 정상).

### 품질 계약 재정의 (CTO 판단)
실키 전 티어 샘플에서 문장 단어 상한(8/12)의 ±1~2 초과와 발표형 9~10문장이
확률적으로 발생. 프롬프트에 분할 예시·하한 강조를 보강하되, **강제 후처리
(자동 문장 절단)는 실전 전날 품질 리스크로 도입하지 않음**. 상한의 목적
(글랜스 가독)은 이제 끊어 읽기 슬래시가 담당하므로, 계약을 "목표 8/12 ·
허용 +2, 발표형 하한 9문장"으로 명문화한다.

### 전 스위트 결과
e2e(오류 0)·rag 15/15·ooc 15/15(.py·.ts)·10분 소크(429 0·번역 54/54·
p95 828ms·JS 0)·resilience(창 정정 후 재검증) — 통과.
개인정보·실키 하드코딩·버전 정합(§33 항목) 재확인 통과.
FIELD-TEST에 "실전 당일 최종 체크 10줄"(v4.1 기준) 추가.
버전 v4.2 · SW 캐시 v23.

## 45. 🎦 프롬프터 레이아웃 — 미팅 화면 위 2패널 (2026-08-25, v4.3)

**요구**: 레퍼런스(The CEO Staff 광고)처럼 회의 화면이 그대로 보이는 위에
[답변 프롬프터 + 상대 발화 통역] 패널이 뜨는 구성.

### 구현 (CSS만 — 🎦 배경 비추기가 켜지면 자동 전환)
- 세로 스택 → **절대 배치 2패널**: 답변 프롬프터(우상, ≤46%폭) ·
  자막 통역(좌하, ≤50%폭) · 고스트 액션+라운드 입력(우하) ·
  요약 스트립(좌상). 패널 사이는 투명 — 캡처된 미팅 화면(얼굴)이 보인다.
- **자막은 '통역'이 주인공**: 한국어 크게·밝게(#EAF0F7, 600), 영어는 보조
  (작게·회색) — 레퍼런스와 동일한 위계.
- PiP 창을 줌 창 크기로 키우면 레퍼런스와 같은 화면이 된다.
  배경 비추기 OFF면 기존 세로 스택 그대로(회귀 없음).

### 부수 발견·수정: e2e 오염 방지
진단 중 e2e '번역' 케이스 연속 실패 — 원인은 mock 오류 주입이 이전
비정상 종료 실행에서 armed로 잔류(파이프 조기 종료로 arm~disarm 사이
사망 가능). 최소 재현으로 제품 정상 확인 후, e2e에 ① 시작 시 주입 강제
해제 ② 주입 창 try/finally를 넣어 구조적으로 차단.

### 검증
미팅 화면 시뮬(4분할 통화 배경) 960px 스크린샷으로 레퍼런스 구성 재현
확인, e2e 신규 기하 케이스(자막 좌·답변 우 분리) 포함 전체 통과·오류 0.
버전 v4.3 · SW 캐시 v24.

## 46. 짧은 질문 투기 적중률 + mock 주입 TTL (2026-08-27, v4.4)

**피드백**: 답변셋 3~5초로 재둔화 (번역은 현재 유지 요청).
**진단**: 오프너 굶주림 가설은 실측 기각(번역 2건 점유 중에도 1.37s).
실제 구성 = 음성 확정 대기(1~2s, 제어 불가) + **투기 미적중 시 재생성**.
범인은 v3.8의 재투기 히스테리시스(새 어절 ≥4): 면접 질문 대부분이
8~15단어로 짧아 마지막 성장분(1~3어절)을 놓침 → 최종 문장과의 일치율
(covers)이 짧은 질문에서 무너져 미리 만든 답을 버리고 재생성.

### 수정 (app.html 2곳, 번역 경로 무변경)
- 재투기 히스테리시스 4→2 어절 — 짧은 질문의 끝자락을 따라잡는다.
  좀비 부담은 동시 6 + 낡은 스트림 취소(v3.8)가 흡수(실측 근거).
- 투기 재사용 문턱 0.88→0.85 — 8단어 질문에서 마지막 1어절 차이(7/8=
  0.875)로 재생성되던 경계 제거. 접두(미완) 투기는 covers 구조상 여전히
  재사용 불가 — 품질 안전.

### 테스트 인프라 영구 수정: mock 주입 60초 TTL
e2e '번역' 가짜 실패의 최종 원인 확정 — 파이프 절단 진단 실행이 arm~
disarm 사이에서 죽으면 주입이 armed로 잔류(시작 시 강제 해제로도 커버
안 되는 경로 존재). mock 자체에 armed_at + 60초 자동 만료를 넣어 이
클래스를 영구 제거. e2e 2연속 전체 통과로 확인.

### 검증
e2e ×2 전부 통과(콘솔 오류 0), 3분 소크 429 0·번역 16/16(번역 경로
무영향 확인). 버전 v4.4 · SW 캐시 v25.

## 47. 🎦 한 기둥 레이아웃 — 시선 이동 최소화 (2026-08-27, v4.5, 실전 피드백)

**피드백**: 자막(좌하)↔답변(우상)의 대각선 시선 왕복이 피로하다.
**수정 (CSS만)**: 프롬프터 레이아웃을 2패널 분산 → **오른쪽 한 기둥
(≤46%)**: [요약 → 답변 카드 → 자막 1~2줄(카드 바로 아래) → 액션·입력].
답변→자막 시선 이동이 한 뼘이고, 왼쪽 절반은 투명 — 미팅 화면(얼굴)
전용. e2e 기하 케이스를 한 기둥 계약(우측 정렬·세로 스택)으로 갱신.
전체 e2e 통과(콘솔 오류 0). 버전 v4.5 · SW 캐시 v26.

## 48. 실전 사후 분석 — STT 붕괴 근절: Gemini 3.5 Transcribe + 턴 병합 (2026-08-27, v5.0)

**실전 결과 (8/27 Workato HR 스크리닝, 실 트랜스크립트 분석)**: 답변셋이
제때 안 나오고 맥락이 자주 깨졌다. 근본 원인은 LLM이 아니라 **입력(STT)**:

1. **Web Speech가 고유명사를 파괴** — "Workato→walkato/avocado/RockAuto",
   "MegazoneCloud→mega stone crab", "당근→Tango/Congo Market",
   "resume→ligament", "160M→160mg". 오염된 자막이 번역·질문감지·RAG 검색을
   연쇄로 무너뜨림 (garbage in → garbage out).
2. **발화가 여러 조각 final로 쪼개져 도착** — 질문의 끝조각만 보고 판정하니
   wantsReply가 놓치거나, 조각에 대한 답이 생성됨.

**수정**:
- **서버 STT 1순위 = gemini-3.5-transcribe** (Interactions API, smart 모드):
  `transcribe_gemini()` 신설 — Files 업로드 → 전사 → **업로드 파일 즉시
  DELETE**(민감 음성 위생). **커스텀 어휘 24개 내장**(Workato·MegazoneCloud·
  Daangn·TuneSystem·iPaaS·OTE 등) + `STT_VOCAB` env로 추가. /api/stt 체인:
  gemini → local-whisper → groq (실패 시 자동 폴백, 응답에 engine 표기).
  실키 검증: 합성 음성에서 "Workato"/"Megazone Cloud" 정확 인식 확인.
- **🎧 마이크 정밀 엔진 신설** — 맥에서 Zoom "앱" 창은 오디오 공유가 안 돼
  탭 오디오를 못 쓴다. 기존 BlackHole 라우팅 그대로, Web Speech 대신
  VAD→/api/stt(gemini)로 인식하는 세 번째 엔진. 트레이드오프: interim이
  없어 투기 선생성은 쉬고 ⚡오프너→본답변이 속도를 커버.
- **턴 병합** — 4초 내 이어지는 상대 발화를 한 턴(≤700자)으로 묶어 질문
  판정·자동 제안·[💬 그냥 답하기]가 턴 전체를 보게 함. 자막 표시는 조각
  그대로, stale 표시는 4단어 미만 조각엔 걸지 않음.

**검증**: 전체 e2e 통과(콘솔 오류 0, 본답변 1.2~2.0s — mock 상대측정).
중간에 나온 429 플레이크는 mock 기동 env 오기(MOCK_RPM_LITE/FLASH)였음 —
앱 결함 아님. 버전 v5.0 · SW 캐시 v27.

## 49. 지연 폭발 근절 — 턴 정착·오프너 분리·모델 고정 (2026-08-27, v5.1)

**증상(사용자 보고)**: "실시간 번역도 잘 안될 뿐더러, 실시간 답변셋도 응답하는데
시간이 오래 걸린다." v5.0 이후 두 가지가 **동시에** 나빠졌다 = 공통 원인 의심.

### 원인 1 — 턴 병합이 조각마다 재발사 (v5.0 자충수)

`wantsReply(turnBuf)`는 턴 버퍼에 '?'가 한 번 들어오면 이어지는 모든 조각에서
참을 유지한다. 그래서 조각이 올 때마다 `suggest()`가 다시 나가 **직전 생성을
abort하고 처음부터** 시작했다. 재현 실험(조각 4개 질문 1개):

| | 발사 횟수 | 번역 큐 대기 |
|---|---|---|
| v5.0 | **8회** (조각마다 본답변+오프너) | 430ms · 오프너 1137ms |
| v5.1 | **2회** (⚡오프너 1 + 본답변 1) | **0ms** |

버려진 오프너들이 fast 레인(번역과 공유, 슬롯 2개)을 점유한 것이 번역 지연의
정체였다 — 하나의 원인이 두 증상을 만들었다.

**수정**: ① 턴이 '정착'한 뒤 1회만 발사(종결부호가 있으면 250ms, 없으면 1100ms —
조각 간격 실측 0.6~1.0s보다 길게). 기다리는 동안은 ⚡오프너가 이미 첫 문장을
띄워 체감 공백이 없다. ② ⚡오프너를 `suggest()`에서 분리해 **턴당 1회**만.
③ 게이트웨이에 예약 슬롯 — 배경 작업(오프너·투기·요약)은 레인의 마지막 슬롯을
못 채운다. 우선순위는 대기줄만 정할 뿐 진행 중 스트림을 밀어내지 못하기 때문.
④ 턴 버퍼를 문장 단위로 자른다(`slice(-700)`은 단어 중간을 잘라 프롬프트 오염).

### 원인 2 — 모델이 무제한으로 '생각'해 최대 52초

실키 계측에서 `queued=0ms`인데 `latency=52317ms`가 찍혔다 — 게이트웨이가 아니라
공급자 지연. 모델 세대마다 생각을 끄는 파라미터가 **정반대**였다:

| 모델 | `thinkingBudget:0` | `thinkingLevel:minimal` | 설정 없음 |
|---|---|---|---|
| flash-lite (번역·오프너) | **400 거부** | 1.23s · thoughts 0 | 1.47s |
| flash-latest (답변) | 3.86s | **400 거부** | 7.82s · thoughts 911 |

코드는 둘 다에 `budget:0`을 보내 한쪽은 거부당하고, 답변 모델은 생각을 못 막았다.

**수정**: ⑤ 모델별로 후보를 내려가며 협상하고 결과를 캐시(프로세스당 1회).
⑥ 기본 모델을 실측으로 재선정 — `*-latest` 별칭은 생각이 무거운 세대를 가리킨다:

| 모델 | 중앙 | 최대 |
|---|---|---|
| gemini-flash-latest (구 기본) | 4.78s | **13.27s** |
| **gemini-3.6-flash** (신 기본) | **1.97s** | **2.11s** |
| **gemini-3.1-flash-lite** (신 fast) | 1.09s | 1.36s |

실시간 면접에는 예측 가능성이 우선이라 버전을 고정하고, 폐기(404) 시 별칭으로
자동 전환하는 폴백을 넣었다(구글은 실제로 gemini-2.5-flash를 404로 내렸다).

### 원인 3 — 🎧 정밀 모드 STT 왕복 (v5.0 권고의 대가)

Files 업로드 → 전사 2단계라 3초 발화에 **업로드 2.00s + 추론 2.73s = 4.73s**.
**수정**: ⑦ 오디오를 본문에 인라인으로 실어 한 번만 왕복 → **2.3~3.1s**
(업로드 왕복 제거, 파일이 서버에 남지 않아 위생에도 유리). ⑧ VAD 침묵 컷
0.9→0.7초(일찍 잘려도 턴 병합이 복구). ⑨ STT 동시 처리 상한을 CPU 수가 아닌
네트워크 기준으로(Gemini 키가 있을 때 4).
⑩ 그래도 남는 2.5초 때문에, 지연 0인 **고유명사 교정 사전**을 넣어 브라우저 STT를
쓰면서도 이름을 되살린다("mega stone crab"→MegazoneCloud, "avocado"→Workato).
일상어는 건드리지 않는다("I pass on that one" 유지).

### 검증 (실키·면접 길이 시뮬 — 질문 7개, 조각 발화)

| 지표 | v5.0 | v5.1 |
|---|---|---|
| 질문당 suggest 호출 | 3.7회 | **2.1회** |
| 답변 지연 p50 | 2.00s | **0.92s** |
| 답변 지연 **최대** | **15.61s** | **1.13s** |
| 번역 p50 / p95 | 0.70s / 1.18s | **0.61s / 0.85s** |
| 번역 미완 | 0건 | 0건 |

e2e 전부 통과(콘솔 오류 0) · 내성 시뮬 전부 통과 · 답변 품질 실키 확인(근거 인용,
깊이 티어 준수, 수치 정확). 회귀 방지용 e2e 계약 2건 추가(턴 정착 ≤2회 · 고유명사
교정). 버전 v5.1 · SW 캐시 v28.

## 50. 코드 스냅샷 — Claude에 붙여넣어 분석시키기 (2026-08-27, v5.2)

**요청**: "코드를 클로드에게 붙여서 분석할 수 있게, 앱에서 복사·붙여넣기 할 수 있게."

**문제**: 소스가 310KB(app.html만 125KB)라 한 번에 붙여넣기 어렵다. 그리고
아무 생각 없이 통째로 내보내면 **개인 코퍼스(실명 고객사·실적)와 API 키가
외부 대화로 나간다** — 이 프로젝트의 영구 제약을 정면으로 어긴다.

**구현**
- `backend/codebundle.py` — 포함 파일을 **화이트리스트로만** 정한다(제외가 아니라
  포함을 열거). backend/data/(미팅 기록·개인 노트·인증 DB)·logs/는 애초에 목록에
  없다. 그 위에 2차 방어로 키·토큰·이메일 패턴을 마스킹한다.
- 논리 단위(서버 / 프론트 / LLM·게이트웨이 / RAG·프롬프트 / 테스트)로 묶고
  55,000자 상한에서 줄 경계로 자른다 → **7조각**. 각 조각 머리에 "이 앱이 무엇이고
  설계 요점 세 가지가 무엇인지" 안내를 붙여, 한 조각만 봐도 맥락이 서게 했다.
- `tools/code_bundle.py` — `python3 tools/code_bundle.py 2` 로 2번 조각을
  클립보드에 복사(pbcopy), 인자 없이 실행하면 바탕화면에 파일로 저장,
  `all`이면 한 파일로(드래그 첨부용).
- 앱 '자료' 탭에 **조각별 [복사] 버튼**. `/api/code`가 목록·본문을 준다.

**보안 판단 — 로컬 전용 게이트**: `start.sh`는 Cloudflare 터널로 공개 주소를
띄운다. 거기서 소스가 열리면 누구나 앱 내부를 가져간다. 터널을 타고 온 요청은
전달 헤더(X-Forwarded-*/CF-*)가 붙고 Host가 외부 도메인이므로, **둘 다 검사해
로컬(localhost:3799)에서만** 응답한다. 클라이언트 IP로 가르는 방식은 통하지
않는다 — cloudflared가 같은 머신에서 127.0.0.1로 접속하기 때문이다.

**검증**: 로컬 200 / 터널 흉내 403 / 잘못된 번호 400 실측. 조각 본문에
키 패턴·개인 코퍼스 경로가 없음을 확인(`store.db`는 코드의 경로 상수일 뿐).
e2e 계약 5건 추가(목록·본문·마스킹·403 차단·버튼 렌더), 전체 통과(콘솔 오류 0).
버전 v5.2 · SW 캐시 v29.

## 51. 상대 입력 장치 지정 — "내 목소리도 잡힘" 근절 (2026-08-27, v5.3)

**증상**: 상태줄에 `⚠️ MacBook Pro 마이크 (Built-in) — 내 목소리도 잡힘`.
Zoom(상대) 소리만 들어와야 하는데 내장 마이크가 잡혀 내 발화까지 자막·답변을
유발한다.

**원인**: 앱이 `getUserMedia({audio:true})`로 **Chrome 기본 입력**을 그대로
받고 있었다. BlackHole이 설치돼 있어도 Chrome 기본이 내장 마이크면 그쪽이 잡힌다.
기존 대응은 문서(§3.5 ③ Chrome 설정 변경)뿐이라, 설정을 놓치면 그대로 실패했다.

**수정 — 앱이 장치를 직접 지정한다**
- 헤더에 입력 장치 목록(`#mic-dev`). `enumerateDevices()`로 채우고 **BlackHole이
  보이면 자동 선택**, 선택은 localStorage에 기억. `devicechange`로 갱신(BlackHole을
  나중에 설치해도 목록에 뜬다).
- 상대 입력: `deviceId: {exact}` 로 **못 박는다** — Chrome 기본값과 무관해진다.
- 내 목소리(🖥 탭 오디오의 마이크): BlackHole이 **아닌** 장치를 고른다
  (`{ideal}`). BlackHole은 상대 소리 복사본이라 여기 잡히면 상대 발화가 '나'로
  두 번 기록된다.
- 상태 표시를 모드별로 정확하게: 🎧/🖥은 **고른 장치**가 진실, 🎤는 Chrome 기본이
  진실. 초기화를 `startSTT()` 안에서 페이지 로드 시점으로 옮겨 **▶를 누르기 전에**
  장치를 고를 수 있게 했다(`devicechange` 리스너 중복 등록도 함께 제거).

**남는 제약(정직하게)**: Web Speech API는 MediaStream을 입력으로 받지 못해
**🎤 브라우저 모드만은 장치 지정이 불가능하다**. 그 모드에서는 Chrome 기본 입력을
BlackHole로 바꾸는 수밖에 없어서, 툴팁이 아니라 **상태줄 문구 자체**를
"Chrome 기본 입력을 BlackHole로 바꾸세요"로 바꿔 놓쳐도 보이게 했다.

**곁가지 결함 1건**: `paintMicSource()`가 권한 거부 시 조용히 빠져나가
상태줄이 텅 비었다 — 화자 분리 여부를 알 길이 없어진다. 권한/오류 사유를
표시하도록 고쳤다.

**검증**: BlackHole 장치를 주입한 Playwright 실험으로 자동 선택·`exact` 제약·
내 목소리 장치 분리·모드별 문구·선택 기억을 실측. e2e 계약 5건 추가. 이 계약이
의미를 가지려면 브라우저에 입력 장치가 있어야 해서 **e2e 자체를 가짜 오디오
장치 + 마이크 권한으로 기동**하도록 바꿨다(이후 마이크 관련 회귀도 잡힌다).
전체 통과(콘솔 오류 0). 버전 v5.3 · SW 캐시 v30.

## 52. Phase 0 — 근거 없으면 만들지 않는다 + 답변은 영어만 (2026-08-27, v5.4)

성숙도 16개 진단(`docs/IMPROVEMENT_PLAN.md`)에서 가장 취약한 그룹이 **환각**
(14·16·4·8: 없음 2·부분 2)으로 나왔다. 그 사슬의 마지막 방벽부터 세운다.

### ① #16 escalation — 미스면 생성하지 않는다

`prompts.py:188`이 `NEVER dodge ... always give a confident answer`로
**근거 없이도 답을 만들라고 명문화**하고 있었다. 화면 문장은 그대로 발화되므로
이건 거짓 경력 진술로 직결된다.

- 준비된 자료에서 근거를 못 찾으면 **LLM을 아예 호출하지 않는다**(실측 273ms).
- 대신 안전한 상투구 + 확정 사실만 띄운다. 빈 화면은 면접 중 더 위험하다.
- 미스 질문은 `logs/misses.jsonl`에 기록 — 다음 답변 유닛의 입력이다(#13 준비).

**티어 판정에서 두 번 틀렸고, 두 번 다 테스트가 잡았다.**
1. 검색 질의에 **맥락을 섞으니** 앞 주제의 적중이 이번 질문의 근거인 양 새어
   들어와 미스가 감춰졌다 → 티어는 **이번 질문**만으로 판정(짧은 후속 질문은 예외).
2. 그래도 새는 경로가 있었다 — 상위 적중이 **지난 미팅 트랜스크립트**였다.
   원칙적으로 '대본'은 준비한 자료(노트·용어집)다. 트랜스크립트는 기록이지
   답변 근거가 아니다 → 티어 판정을 `sources=["note","glossary"]`로 한정.
   (키워드 검색만 도는 지금은 트랜스크립트가 일반 영어로 폭넓게 걸린다 — §L2-8)

판정 정확도 7/7(코퍼스 밖 2건 C · 안 4건 B · 짧은 후속 B).

### ② #14 quality gate — 자료에 없는 숫자를 붉게

서버가 '자료에 실재하는 수치 집합'을 메타로 내려주고, 클라이언트가 출력의
숫자를 대조해 미검증 값에 표시한다. **차단하지 않는다** — 면접 중 화면이 통째로
비면 대응이 불가능하다. 사람이 1초 안에 "이 숫자는 근거 없음"을 알면 된다(#15).

### ③ 답변은 영어만 (KR·PR 제거)

실사용 확인: **상대 발화는 EN+KR을 모두 보지만 답변셋은 영어만 읽는다.**
안 읽는 줄을 만드느라 EN 완결 이후 1.51초를 더 쓰고 있었다.

| 지표 | v5.3 | v5.4 |
|---|---|---|
| 답변 전체 완결(중앙) | 3.00s | **1.81s** |
| 최대 | 4.22s | **2.02s** |
| 프롬프트 | 8,179자 | **7,527자** |
| 출력 구성 | EN 48%·KR 25%·PR 28% | **EN 100%** |

되돌리려면 `ANSWER_LINES=en,kr,pr bash start.sh`. 끊어 읽기 슬래시는 EN 안에
있으므로 그대로 유지된다.

### 곁가지 결함 1건 — 카드 깜빡임

`renderCard`가 박스를 먼저 비우고 EN이 있을 때만 다시 채웠다. 스트리밍 첫 순간
("EN:"만 도착)엔 파싱 결과가 비어 **카드가 통째로 사라졌다.** 프래그먼트에
만들어 두고 만들 게 생겼을 때만 교체하도록 고쳤다. (e2e가 잡았다 — PiP에서
`#c-answers .en`이 null이 되며 크래시)

### 검증

e2e 전부 통과(콘솔 오류 0), 회귀 계약 5건 추가(미스 배너·생성 안 함·1초 미만·
수치 표시·확정 수치 보존). mock도 프롬프트가 요청한 줄만 내도록 고쳐 계약이
실제로 반영되게 했다. 버전 v5.4 · SW 캐시 v31.

## 53. Phase 1 — 의미검색을 설치 없이, 비용은 필요한 곳에만 (2026-08-27, v5.5)

진단 #8에서 **임베딩이 꺼져 있었다**(218청크 중 0건, "키워드 전용"). RRF
하이브리드가 BM25 단독으로 퇴화해, 면접관이 내 트리거와 다른 어휘로 물으면
놓쳤다. 원래 계획은 `ollama pull bge-m3`였는데 2GB 설치가 필요해 비개발자에겐
부담이다. **이미 있는 Gemini 키로 푼다.**

### 실측이 설계를 정했다

| 항목 | 실측 |
|---|---|
| 질의 1건 임베딩(Gemini) | **427~676ms** (768차원 385ms) |
| 코퍼스 배치 색인 | 16건 0.53s → **219청크 6.7초** |
| BM25 검색 | 1~3ms |

질의당 0.4~0.7초는 **매 질문에 얹을 수 없다**(답변 전체가 1.81초다). 그래서
2단으로 나눴다.

1. **BM25 먼저**(1ms). 적중이 있으면 끝 — 흔한 질문은 임베딩 비용 0.
2. **비었을 때만 의미검색**. 어차피 놓치면 Tier C로 답을 안 만드는 구간이라,
   그 비용을 여기서만 낸다.

로컬 bge-m3가 있으면 왕복이 없으므로 그때는 항상 하이브리드로 돈다
(`search(semantic=None)`의 기본이 "공짜일 때만").

### 벡터 검색의 함정 — 유사도 하한

2단을 붙이자 **Tier C 게이트가 무력해졌다.** 벡터 검색은 아무리 멀어도 최근접
k개를 돌려주므로 "의미로 걸렸다"만으로는 관련성이 아니다. 코퍼스에 전혀 없는
질문도 근거가 있는 것처럼 통과했다.

실측 분포(gemini-embedding-001, 768차원):

| 구분 | 코사인 유사도 |
|---|---|
| 코퍼스 안 · 패러프레이즈 | 0.583 ~ 0.707 |
| 코퍼스 밖 | 0.463 ~ 0.522 |

두 무리 사이인 **0.55**를 하한으로 뒀다(`RAG_MIN_SIM`).

### 결과 — 라우팅 판정 9/9

| 질문 | 판정 | 지연 |
|---|---|---|
| 코퍼스 안 3건 (딜·파이프라인·연봉) | B ✅ | 2~6ms |
| 패러프레이즈 "largest contract you have ever signed" | B ✅ | 1,013ms |
| 패러프레이즈 "comfortable running meetings in English" | B ✅ | 791ms |
| 패러프레이즈 "find brand new customers from scratch" | B ✅ | 11ms |
| 코퍼스 밖 3건 (피자·등산·요리) | C ✅ | 395~806ms |

**"영어로 회의 진행이 편한가"는 v5.4까지 놓치던 질문이다**(진단 §2-3의 1/10 미스).
이제 잡힌다.

검증: e2e 전부 통과(콘솔 오류 0). 미스 경로 계약은 의미 확인 비용을 포함해
2.5초로 조정했다 — 본답변 생성(≈1.8초)보다 싸고, 무엇보다 지어내지 않는다.
버전 v5.5 · SW 캐시 v32.

---

## 54. Phase 2 — 라우팅 골든셋과 소스 위계 (2026-08-29, v5.6)

**한 줄:** 면접 질문 40개를 기준선으로 고정한 뒤 검색을 고쳤다. top3 적중
66% → **100%**, top1 42% → **87%**.

### 왜 이걸 먼저 했나

v5.5까지의 점검은 10문항이었고 9/10이었다. 그래서 "잘 된다"고 믿었다.
Workato 하이어링 매니저가 실제로 파고들 축(딜 딥다이브·파이프라인·숫자·
방법론·경쟁)으로 40문항을 만들어 다시 재니 **top3 25/38 (66%)** 이었다.
앞선 10문항이 쉬운 문항이었을 뿐이다.

기준선이 없으면 어떤 수정도 개선인지 개악인지 알 수 없다. 그래서
`tests/golden_routing.py`를 먼저 만들었다. 세 가지를 함께 잰다.

- **top1** — 첫 근거가 기대한 대본인가 (Tier A 조회의 전제)
- **top3** — 기대 대본이 근거 안에 들어오는가 (Tier B 생성의 전제)
- **티어 정확도** — 대본 있는 질문을 C로 떨구거나, 없는 질문을 B로 새게 하지 않는가

### 고친 것 두 가지

**1. 소스 위계 (`backend/rag.py`)** — 지난 미팅 트랜스크립트가 내 대본보다
위로 올라오고 있었다. 트랜스크립트는 "내가 그때 이렇게 말했다"는 기록이지
"이렇게 말해야 한다"는 대본이 아니다. RRF 융합 뒤 가중치를 곱한다.

```python
SOURCE_WEIGHT = {"note": 1.0, "glossary": 0.55, "transcript": 0.40}
```

**2. 질의 확장 (`backend/triggers.py`, 신규)** — 면접관의 표현과 내 자료의
어휘가 다르다. "lost deal"이라 묻는데 자료에는 "실패에서 배운 것"으로 적혀
있다. 11개 패턴으로 **질의에만** 자료 어휘를 덧붙인다. 프롬프트에는 넣지
않으므로 **사실을 추가하지 않는다** — 검색어만 넓힌다.

### 측정 경과

| 단계 | top1 | top3 | 티어 |
|---|---|---|---|
| 베이스라인 | 16/38 (42%) | 25/38 (66%) | 40/40 |
| + 소스 위계 | 24/38 (63%) | 28/38 (74%) | 40/40 |
| + `RAG_WEAK_TERMS=3` | 25/38 (66%) | 29/38 (76%) | 40/40 |
| + 질의 확장 (임계 2) | **33/38 (87%)** | **38/38 (100%)** | **40/40** |

`RAG_WEAK_TERMS`는 **2로 되돌렸다**. 3으로 올리면 의미검색 발동률이
22% → 65%, 검색 지연 중앙값이 4ms → 689ms로 뛰는데 정확도는 +2%p뿐이다.
비용이 대가에 맞지 않는다.

### 지연 회귀 없음

```
검색 지연 중앙 4ms · 최대 1004ms
임베딩 발동 6/40 (15%)
```

질의 확장이 BM25 적중을 늘려 **의미검색 발동률이 오히려 22% → 15%로 내려갔다**.
비싼 경로를 덜 타게 된 것이다.

### 알아둘 것 — 의미검색 2단은 하중을 받는 부품이다

`GEMINI_API_KEY` 없이 돌리면 2단이 꺼지고 성적이 이렇게 떨어진다.

| | top1 | top3 | 티어 |
|---|---|---|---|
| 키 있음 | 33/38 (87%) | 38/38 (100%) | 40/40 (100%) |
| 키 없음 | 29/38 (76%) | 34/38 (89%) | 36/40 (90%) |

실전에서는 키가 항상 있으므로 문제되지 않지만, **골든셋을 돌릴 때 키를
빠뜨리면 없는 회귀를 보게 된다.** README에 적어 뒀다.

### 회귀 검증 — 변경 전후 동일

`SOURCE_WEIGHT`가 기존 계약을 건드릴 수 있어 stash로 전후를 대조했다.

| 스위트 | 변경 전 | 변경 후 | 판정 |
|---|---|---|---|
| `tests/e2e.mjs` | — | 전부 통과 (콘솔 오류 0) | ✅ |
| `tests/rag_eval.py` | 8/15 | 8/15 (문항별 동일) | 회귀 없음 |
| `tests/ooc_eval.py` | 10/15 | 10/15 (문항별 동일) | 회귀 없음 |

**`rag_eval`·`ooc_eval`의 실패 문항은 이번 변경 탓이 아니다.** 두 스위트는
초기 영어 시드 코퍼스(`current role summary`, `iPaaS in one line` 등)를
기대하는데, `--replace` 임포트 이후 저장소는 한국어 수업 노트 + Workato
코퍼스로 바뀌었다. 기대 문자열이 가리키는 시드가 이제 존재하지 않는다.

실제로 `ooc_eval`의 C 실패 1건("Why are you leaving your current role")은
**"이직 사유 — 시그니처 답변 (Why Workato)"를 정확히 찾아온 것**인데,
스위트가 그 질문을 코퍼스 밖으로 분류해 두어 감점된다. 오답이 아니라
채점표가 낡았다.

→ 면접 축의 기준선은 이제 `golden_routing.py`가 맡는다. 두 스위트의 기대값
현행화는 코퍼스가 안정된 뒤 별도로 한다(지금 고치면 채점표를 답에 맞추는
셈이 된다).

버전 v5.6 · SW 캐시 v33.

---

## 55. Tier A — 검수된 대본을 생성 없이 그대로 (2026-08-29, v5.7)

**한 줄:** 미리 검수한 영어 판본이 있는 질문은 LLM을 부르지 않는다. **6~12ms**에
뜨고, 문장이 매번 같다.

### 왜 필요했나

사용자의 전제는 "답변을 그대로 읽는다"였다. 그런데 코퍼스의 핵심 자산인
딜 스토리 A~H는 **한국어 S/A/R 속기**다("갱신이 커머디티화될 위기, 트랜잭셔널
리셀러 관계"). 읽을 수 있는 문장이 아니다.

매번 LLM에 영작을 시키는 것으로 메우고 있었는데, 그 방식은 세 가지가 나쁘다.

1. 느리다 — TTFT 0.82초
2. **매번 문장이 달라진다** — 그대로 읽을 문장이 매번 다르면 리허설이 무의미하다
3. 숫자가 흔들린다

### 구조

`backend/units.py` — 노트 제목으로 이어 붙이는 별도 파일
(`backend/data/imported/answer_units.json`, gitignore 대상). 코퍼스를
`--replace`로 재적재해도 유닛은 유지된다.

유닛 하나에 30초 판본과 90초 판본, 이 답변이 말해도 되는 수치(`key_numbers`),
그리고 `reviewed` 플래그가 들어간다. **`reviewed: false`면 Tier A로 나가지
않는다** — 검수 안 된 문장이 그대로 발화되는 것이 이 기능의 유일한 큰 위험이라,
기본값을 잠가 뒀다.

### 실측 결함 — Tier A 게이트는 Tier B보다 엄해야 한다

첫 구현은 "1순위 근거에 유닛이 있으면 연다"였다. 그러자 이런 게 나왔다.

```
Q: "What would you ask us about how the team works day to day?"
→ [A] 첫 90일 계획 패턴          ← 틀린 대본. 'day'가 겹쳤을 뿐이다.
```

top1 정확도가 89%라는 건, **11%는 틀린 대본을 자신 있게 읽는다**는 뜻이다.
Tier B는 LLM이 질문과 근거 3개를 함께 보므로 이런 미스를 어느 정도 흡수한다.
Tier A에는 흡수 장치가 없다.

실측 분포가 답을 줬다 — 맞는 1순위의 `match_terms`는 5~9, 틀린 1순위는 2였다.
그래서 Tier A 전용 게이트를 뒀다(`UNIT_MIN_TERMS=4`).

| 임계값 | 발동 | 맞음 | **틀림** |
|---|---|---|---|
| 2 | 27 | 24 | **3** |
| 3 | 20 | 19 | **1** |
| **4** | **12** | **12** | **0** |
| 5 | 11 | 11 | 0 |
| 6 | 10 | 10 | 0 |

4가 무릎이다 — 오발화 0을 유지하는 최대 커버리지. 커버리지가 낮은 건 견딜 수
있다(Tier B로 떨어질 뿐). **오발화는 못 견딘다.**

### 속도

| 경로 | 첫 화면까지 |
|---|---|
| Tier A (검수 대본) | **6~12ms** |
| Tier B (생성) | 820ms (TTFT) |
| Tier C (미스) | 1.2초 |

### 라우팅 — 유닛 점검이 코퍼스 결함을 잡았다

`tools/check_units.py`를 만들어 유닛마다 4가지를 검사했다(노트 연결 · 수치 근거 ·
라우팅 · 발화 길이). 이게 세 가지를 잡아냈다.

1. **중복 주제 노트** — "영어 리스크"와 "영어 커뮤니케이션"이 같은 주제로 경합.
   유닛을 검색이 실제로 집는 쪽에 붙였다. 랜드앤익스팬드도 같은 문제였다.
2. **`why workato` 미스** — 골든셋의 top1 미스 중 하나. 기존 트리거 패턴이
   `leaving`·`motivating`만 잡고 "Why Workato, and why now?"를 놓쳤다.
3. **`biggest deal`이 작은 딜 노트로** — 최대 딜은 EDP 갱신 쪽인데 랜드앤익스팬드
   노트가 이겼다.

트리거 3개를 추가해 유닛 라우팅 20/20, 골든셋 top1이 87% → **89%**로 올랐다.

### 검증

| 스위트 | 결과 |
|---|---|
| `tests/golden_routing.py` | top1 34/38 (89%) · top3 38/38 (100%) · 티어 40/40 · **Tier A 오발화 0** |
| `tools/check_units.py` | 유닛 20개 · 형식 0건 · 라우팅 20/20 |
| `tests/e2e.mjs` | 전부 통과 (Tier A 계약 4건 신규) · 콘솔 오류 0 |
| `tests/rag_eval.py` | 8/15 (변경 전과 동일) |
| `tests/ooc_eval.py` | 10/15 (변경 전과 동일) |

골든셋 러너에 **Tier A 오발화를 실패 조건으로 넣었다** — 틀린 대본이 하나라도
뜨면 스위트가 실패한다. 커버리지 저하는 실패가 아니다.

### 남은 것 — 사용자 검수

유닛 20개는 초안이고 전부 `reviewed: false`다. 사실관계와 말투는 본인만 판단할
수 있다. 검수 전까지 Tier A는 한 번도 발동하지 않는다(골든셋 실측: 발동 0/38).

버전 v5.7 · SW 캐시 v34.

---

## 56. 의도 게이트 — 어휘가 겹친다고 답이 되지는 않는다 (2026-08-29, v5.8)

**한 줄:** §55의 어휘 게이트를 통과하고도 **묻지 않은 질문에 답하는 대본**이
2건 열려 있었다. 유닛이 선언한 의도(`intent_tags`)를 게이트로 승격해 막았다.

### 무엇을 놓쳤나

§55에서 `match_terms >= 4` 게이트로 오발화를 0으로 만들었다고 적었다. 그 판정의
근거는 골든셋의 `expect` 목록이었는데, **그 목록이 너무 헐거웠다.** 여러 노트
제목을 허용하다 보니 "이 대본이 이 질문에 답하는가"를 묻지 못했다.

12건을 하나씩 소리 내어 읽어 보고 2건을 찾았다.

```
Q: "What does your outbound motion actually look like day to day?"
→ [A] 네트워크 없이 파이프라인 만들기
   읽는 문장: "If I couldn't lean on my network at all, I'd still build pipeline…"
```

면접관은 **평소 하는 방식**을 물었는데 대본은 **묻지도 않은 가정**("네트워크를
못 쓴다면")에 답한다. 그대로 읽으면 회피로 들린다. 어휘는 충분히 겹쳤다
(network, pipeline, outbound) — 그래서 어휘 게이트로는 못 걸렀다.

두 번째는 "How would you build a territory plan for Korea?"가 시장 진단 대본을
읽는 건이었다. 계획을 물었는데 진단을 답한다.

### 고친 것

`intent_tags`는 원래 "사람이 읽는 메모 · 검색엔 안 씀"이라고 주석에 적혀 있었다.
이걸 **게이트로 승격**했다(`units.matches_intent`). 판정은 **구(phrase) 단위
완전 일치**다 — 낱말 하나 겹침으로 여는 방식은 같은 실수를 반복한다.

Tier A는 이제 둘 다 통과해야 열린다.

| 게이트 | 묻는 것 |
|---|---|
| `match_terms >= 4` | 이 노트가 맞는가 (어휘) |
| `intent_tags` 구 일치 | 이 대본이 **이 질문에** 답하는가 (의도) |

내가 쓴 태그도 감사했다. `territory`, `automation`, `network`, `enterprise`,
`channel` 같은 **한 낱말 태그 11개를 걷어냈다** — 다른 질문에서도 흔히 쓰이는
말이라 의도의 증거가 못 된다. 특히 `territory`는 한국 시장 노트에 잘못 달려
있었다(테리토리 전략 노트가 따로 있다). 대신 구 단위 태그로 메웠다.

### 결과 — 커버리지를 절반으로 줄여 정확도를 샀다

| | 발동 | 승인된 것 | 미승인 |
|---|---|---|---|
| §55 (어휘만) | 12 | 10 | **2** |
| §56 (어휘+의도) | **6** | **6** | **0** |

골든셋에 **승인 목록(`UNIT_OK`)** 을 넣었다. 여기 없는 유닛이 Tier A로 뜨면
스위트가 실패한다. 게이트를 둘 다 통과해도 "그 대본이 이 질문에 답하는지"는
결국 사람이 읽어 봐야 알기 때문이다 — 두 번 데고 나서 배웠다.

### 줄어든 커버리지는 버려지지 않는다

Tier A로 안 열린 유닛도 **Tier B의 근거로 들어간다.** 원본 노트는 한국어 S/A/R
속기라 모델이 매번 새로 영작하는데, 검수된 영어 판본을 주면 그 말투를 그대로
쓴다. 프롬프트에 `REVIEWED SPOKEN VERSION` 표식과 함께 넣고 "이 사람이 실제로
말하는 방식이니 재번역하지 말라"고 지시한다.

실측 — 아까 잘못 열리던 두 질문의 **생성 결과**:

```
Q: What does your outbound motion actually look like day to day?
→ "For me, hunting is not about cold calls… Day to day, I look for business
   triggers on LinkedIn and news. Then I find the warm path in using internal
   account managers. I multi-thread from day one… Cold outreach is my last
   resort, not my first move."                                   (2.04s)
```

묻은 것에 답하면서 검수된 말투를 유지한다. 틀린 대본을 그대로 읽는 것보다
낫고, 한국어 속기에서 매번 새로 영작하는 것보다도 낫다.

`units.find()`가 미검수 유닛을 걸러내므로 **검수 전에는 Tier B 동작도 바뀌지
않는다.** 검수를 마치면 두 티어가 함께 좋아진다.

### 검증

| 스위트 | 결과 |
|---|---|
| `golden_routing.py` | top1 34/38 (89%) · top3 38/38 (100%) · 티어 40/40 · **Tier A 미승인 0** |
| `check_units.py` | 유닛 20개 · 형식 0건 · 라우팅 20/20 |
| `e2e.mjs` | 전부 통과 · 콘솔 오류 0 |
| `rag_eval.py` / `ooc_eval.py` | 8/15 · 10/15 (둘 다 변경 전과 동일) |

버전 v5.8 · SW 캐시 v35.
