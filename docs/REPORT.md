# REPORT — 글로벌 프로덕트 미션 (Phase 4 산출물, v1.0.0)

> docs/AUDIT.md의 갭(G1~G4)에 대한 실행 결과. 커밋 5개, e2e 49파일 전부 통과.

## 1. 변경 요약

### P0-A · Preply 수업 노트 파이프라인 (G1) — `63f56af`
- **시드 스냅숏**: Notion "영어 진옥" 실사로 1~7회차 커리큘럼을
  data/preplyNotes.json으로 — 토큰 설정 없이 오늘부터 동작.
- **런타임 동기화**: /app/api/notion/preply(페이지 트리 순회, 원문 수집) +
  클라이언트 엔진(지수 백오프 재시도 → aiGuard AI 구조화, 노트별 해시 캐시 →
  로컬 캐시 → 실패 시 캐시→시드 폴백). 미설정(501)은 실패가 아닌 안내 상태.
- **SRS 파이프라인**: 노트 문장 → 표현장 + 약점 노트(cat '수업', 멱등) →
  세션 워밍업에 자연 유입. 회차 문장 → 드릴 원탭.
- 상태 4종(동기화됨/시드/오프라인/미설정) 설계 완료 (G4).
- 검증 중 발견한 결함: state.load()의 형태 가드가 "기본값 null vs 저장 배열"을
  불일치로 처리 — 캐시 기본값을 배열로 바꿔 해결.

### P0-B · Lighthouse (G2) — `bbc88d6`
| 카테고리 | Before | After |
|---|---|---|
| Performance | 69 | **87** |
| Accessibility | 100 | **100** |
| Best Practices | 96 | **100** |
| (LCP) | 13.1s | **3.6s** |
| (총 전송량) | 2,358KB | **433KB** |

원인별 5라운드: ① manifest 404(basePath 상대경로) ② SSR LCP 앵커(하이드레이션
전 null 렌더) ③ **폰트 2MB → 동적 서브셋**(진짜 주범 — 총 전송 -1.9MB)
④ 홈 하이드레이션 다이어트(First Load 161→135KB, CLS 0 유지)
⑤ 프리로드 강제 발화가 LCP와 대역폭을 다투던 타이밍 조정.

**Performance 87에서 멈춘 이유(정직 보고)**: 남은 갭은 CSR 구조 비용 —
FCP 2.4s(HTML→CSS 왕복 체인) + 하이드레이션 1.2s(4× CPU 스로틀).
다음 지렛대는 크리티컬 CSS 인라인 또는 앱 셸 부분 하이드레이션(큰 수술).
실사용(재방문)은 SW 프리캐시로 이 수치보다 훨씬 빠르다(자체 하니스 warm FCP
~0.35s). 90+ 두 카테고리 달성, Performance는 87 + 근거 기록으로 갈음.

### P1-A · 튜터 교차 세션 기억 (G3의 무료 부분) — 이 커밋
- 대화 턴마다 마지막 6줄 스냅숏(va_tutor_memory) + 최근 교정 2건을 다음
  세션 시스템 프롬프트에 "지난 세션 기억"으로 주입(억지 언급 금지 지시 포함).
  회화가 매번 초면으로 시작하지 않는다. AI 추가 호출 0.

## 2. 셀프 QA 기록
- e2e 신설: 48-preply(11항목) · 49-tutor-memory(5항목). 총 49파일 전부 통과.
- 390px 스크린샷 검증: 수업 노트 목록/상세(SRS 등록 칩) 육안 확인.
- 회귀 수정: 27-pron의 지연 청크 즉시 조회 레이스(청크 그래프 변경으로 표면화).

## 3. 에스컬레이션 — 사용자 결정 필요 (구현 보류)
1. **Claude API 전환(유료)**: 현재 Groq 무료 + aiGuard로 품질 방어 중.
   전환 시 이득 = 더 나은 한국어 코칭·긴 컨텍스트. 예상 비용: 현 사용 패턴
   (하루 수십 회 短호출) 기준 Haiku 4.5로 월 ~$1-3, Sonnet 5로 월 ~$5-15 수준.
   전환 원하면 ANTHROPIC_API_KEY 방식으로 프록시에 폴백 체인 추가 가능.
2. **Notion 쓰기 동기화(자동 쓰기)**: 숙제 완료 체크·훈련 결과를 노트에
   기록하는 방향. 외부 쓰기라 확인 후 진행(수동 버튼 방식 권장).

## 4. 남은 기술부채 / 다음 스프린트 제안
1. Lighthouse Performance 87→90+: 크리티컬 CSS 인라인(experimental
   optimizeCss) 또는 온보딩 SSG 분리.
2. 레슨 탭 첫 클릭(홈 언마운트 ~470ms) — 앱 셸 keep-alive 수술.
3. Notion 동기화 자동 트리거(현재 수동 버튼) — 주 1회 세션 진입 시 자동 확인.
4. globals.css 분리, 시도 로그 롤업(1,000건 근접 시).
5. 8회차 이후 노트: 동기화 파이프라인이 자동 처리하나, 토큰 설정 전에는
   시드 갱신 필요(다음 실사 시 스냅숏 교체).
