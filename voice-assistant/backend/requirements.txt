# ─────────────────────────────────────────────────────────────
# TBLT LMS — 백엔드 AI 스피치 파이프라인 의존성
# (클라우드 이전용 풀스택 경로. 로컬 실행은 server.py로 의존성 0)
# ─────────────────────────────────────────────────────────────

# Web 프레임워크 / ASGI
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==12.0

# AI 오케스트레이션 (CAF 분석 체인)
langchain==0.3.0
langchain-core==0.3.0
langchain-community==0.3.0
langchain-ollama==0.2.0

# 정형 데이터 (RDBMS)
sqlalchemy==2.0.34
psycopg2-binary==2.9.9
alembic==1.13.2          # 비파괴적 마이그레이션

# 비정형/벡터 데이터
qdrant-client==1.11.1    # 발화 임베딩 / 의미 검색
pymongo==4.8.0           # 원문 트랜스크립트 로그

# 대용량 미디어 (오디오)
boto3==1.35.0            # AWS S3

# STT (실시간 오디오 → 텍스트, /ws/audio 용)
faster-whisper==1.0.3

# 공통
pydantic==2.9.0
httpx==0.27.2
