# REPORT — 자동화 훈련 루프 고도화 (v0.94.0 → v0.96.0)

> PLAN.md의 실행 결과 회고. 미션: "말이 입에서 안 나오는" 자동화 부족을
> 훈련 루프로 푼다. 전 단계 완료, e2e 44파일 전부 통과.

## 1. 변경 사항 요약

### Phase 1 — 복습 엔진 (P0) · commit `993ea5f`
- **시도 로그**(`va_attempt_log`, 상한 1000): 모든 말하기 채점이 문장·점수·
  지연·길이·출처와 함께 남는다. 기존 키는 스키마 불변 — 데이터 유실 위험 0.
- **패턴 SRS**(`va_pattern_srs`): 정착 패턴을 D+1/D+3/D+7/D+16/D+35에 실전
  리콜로 재소환. 기존 정착분은 멱등 시드 마이그레이션(원본 비파괴, e2e로 검증).
- 세션 워밍업 = 통합 큐(문장 SRS 2 + 패턴 리콜 1). 리콜 채점이 박스를 움직인다.

### Phase 2 — 자동화 루프 (P0) · commit `993ea5f`
- **발화 개시 지연**: STT의 RMS 루프가 이미 감지하던 첫 유성 시점을
  `voiceOnsetMs`로 노출. 점수 옆 ⚡배지 + 로그 기록. 절대 기준 없이 본인 추이만.
- **2연속 규칙**: 세션 원어민 단계는 80점 2회 연속(●○ 표시)이어야 통과.
  같은 문장의 두 번째 발화가 빨라지는 경험이 곧 자동화 훈련.

### Phase 3 — 전이 루프 (P1) · commit `8a8e490`
- 세션 완주 → **"오늘 패턴으로 2분 대화"** 회화 핸드오프.
- **패턴 사용 감지**: 40개 stem의 결정적 문자열 매칭(AI 아님) —
  `va_pattern_use` 누적 + 세션당 1회 축하.
- **교정 축적**(`va_mistakes`, 상한 200): 회화 배경 교정의 틀린 문장이
  휘발되지 않는다. 성장 화면 "맞춤 훈련" 카드 → 고친 문장 드릴.

### Phase 4 — 훈련 대시보드 (P1) · commit `6066a48`
- 진도 화면 최상단: 2주 발화 · 평균 정확도 · 오늘 입 트임 3숫자 +
  정확도 막대(7일) + 입 트임 라인(지연이 줄면 내려오는 표준 축) +
  약점(발음 축)·실전 사용·틀린 문장 드릴 진입.
- UI 시안 2종 비교(히어로형 채택 — "지금 상태"에 즉답).

### Phase 5 — 주간 말하기 시험 (P2) · 이 커밋
- 7일마다 홈 배너 → 이번 주 패턴 3개를 넣어 1분 자유 발화 →
  단어·WPM·패턴 사용 기록, 지난주와 비교. 측정 리추얼(채점 아님).

## 2. 검증 기록
- 매 단계: tsc · 프로덕션 빌드 · e2e 전체(신규 가드 40~44, 총 46개 항목 추가)
  · Playwright 스크린샷 육안 확인.
- 스크린샷 검증에서 잡아 고친 것: 대시보드 variant 클래스명이 내부
  `.dash-hero`와 충돌해 레이아웃이 가로로 눕던 문제, 막대 라벨 잘림,
  라인 차트 축 방향, 실전 화면 상황 지문 중복.
- 데이터 보존: 구버전 키만 있는 상태를 시드한 e2e에서 원본 무변경 +
  시드 멱등성 확인(40-review).

## 3. 남은 개선점 (기술 부채)
- `globals.css` 단일 파일 ~7,900줄 — 모듈 분리 후보.
- `lessons.json` 199KB 정적 임포트 — 회화·레슨 탭 전환(~600-870ms)의 근원.
  동적 로드로 바꾸면 탭 전환 체감이 크게 개선될 것.
- e2e 직렬 실행 ~8분 — 파일 병렬화(서버 공유) 여지.
- 시도 로그 집계는 읽기 시 계산 — 로그가 1,000건에 근접하면 일별 롤업
  캐시 도입 검토.

## 4. 다음 이터레이션 제안
1. **출퇴근 오디오 모드**: 오늘의 스토리 대화 + due SRS 문장을 이어서 자동
   재생(입력량 확충 — 현재 앱은 출력 편중).
2. **리콜 확장**: 패턴 리콜을 세션 밖(드릴 탭)에서도 몰아서 할 수 있는
   "리콜 러시" 모드.
3. **입 트임 목표**: 지연 추이가 4주 쌓이면 "지난달 중앙값 -20%" 같은
   본인 기준 목표 제시(절대 기준은 계속 금지).
4. **교정 유형 분류**: va_mistakes에 유형 태그(시제/전치사/관사)를 붙여
   약점 카드를 발음 축과 같은 해상도로.
5. 기술 부채 상환(위 3장) — 특히 lessons.json 동적화.
