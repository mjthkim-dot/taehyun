# REPORT — 자동화 훈련 루프 고도화 (v0.94.0 → v0.96.0)

> PLAN.md의 실행 결과 회고. 미션: "말이 입에서 안 나오는" 자동화 부족을
> 훈련 루프로 푼다. 전 단계 완료, e2e 44파일 전부 통과.

## 1. 변경 사항 요약

### Phase 1 — 복습 엔진 (P0) · commit `993ea5f`
- **시도 로그**(`va_attempt_log`, 상한 1000): 모든 말하기 채점이 문장·점수·
  지연·길이·출처와 함께 남는다. 기존 키는 스키마 불변 — 데이터 유실 위험 0.
- **패턴 SRS**(`va_pattern_srs`): 정착 패턴을 D+1/D+3/D+7/D+16/D+35에 실전
  리콜로 재소환. 기존 정착분은 멱등 시드 마이그레이션(원본 비파괴, e2e로 검증).
- 세션 워밍업 = 통합 큐(문장 SRS 2 + 패턴 리콜 1). 리콜 채점이 박스를 움직인다.

### Phase 2 — 자동화 루프 (P0) · commit `993ea5f`
- **발화 개시 지연**: STT의 RMS 루프가 이미 감지하던 첫 유성 시점을
  `voiceOnsetMs`로 노출. 점수 옆 ⚡배지 + 로그 기록. 절대 기준 없이 본인 추이만.
- **2연속 규칙**: 세션 원어민 단계는 80점 2회 연속(●○ 표시)이어야 통과.
  같은 문장의 두 번째 발화가 빨라지는 경험이 곧 자동화 훈련.

### Phase 3 — 전이 루프 (P1) · commit `8a8e490`
- 세션 완주 → **"오늘 패턴으로 2분 대화"** 회화 핸드오프.
- **패턴 사용 감지**: 40개 stem의 결정적 문자열 매칭(AI 아님) —
  `va_pattern_use` 누적 + 세션당 1회 축하.
- **교정 축적**(`va_mistakes`, 상한 200): 회화 배경 교정의 틀린 문장이
  휘발되지 않는다. 성장 화면 "맞춤 훈련" 카드 → 고친 문장 드릴.

### Phase 4 — 훈련 대시보드 (P1) · commit `6066a48`
- 진도 화면 최상단: 2주 발화 · 평균 정확도 · 오늘 입 트임 3숫자 +
  정확도 막대(7일) + 입 트임 라인(지연이 줄면 내려오는 표준 축) +
  약점(발음 축)·실전 사용·틀린 문장 드릴 진입.
- UI 시안 2종 비교(히어로형 채택 — "지금 상태"에 즉답).

### Phase 5 — 주간 말하기 시험 (P2) · 이 커밋
- 7일마다 홈 배너 → 이번 주 패턴 3개를 넣어 1분 자유 발화 →
  단어·WPM·패턴 사용 기록, 지난주와 비교. 측정 리추얼(채점 아님).

## 2. 검증 기록
- 매 단계: tsc · 프로덕션 빌드 · e2e 전체(신규 가드 40~44, 총 46개 항목 추가)
  · Playwright 스크린샷 육안 확인.
- 스크린샷 검증에서 잡아 고친 것: 대시보드 variant 클래스명이 내부
  `.dash-hero`와 충돌해 레이아웃이 가로로 눕던 문제, 막대 라벨 잘림,
  라인 차트 축 방향, 실전 화면 상황 지문 중복.
- 데이터 보존: 구버전 키만 있는 상태를 시드한 e2e에서 원본 무변경 +
  시드 멱등성 확인(40-review).

## 3. 남은 개선점 (기술 부채)
- `globals.css` 단일 파일 ~7,900줄 — 모듈 분리 후보.
- `lessons.json` 199KB 정적 임포트 — 회화·레슨 탭 전환(~600-870ms)의 근원.
  동적 로드로 바꾸면 탭 전환 체감이 크게 개선될 것.
- e2e 직렬 실행 ~8분 — 파일 병렬화(서버 공유) 여지.
- 시도 로그 집계는 읽기 시 계산 — 로그가 1,000건에 근접하면 일별 롤업
  캐시 도입 검토.

## 4. 이터레이션 2 — 완료 (v0.97.0)

3장의 제안 중 4건을 이어서 구현:
- **🎧 오디오 모드**: 오늘 패턴 대화 + due SRS + 정착 패턴을 영어→한국어 뜻
  순서로 이어 읽는 듣기 루프(전부 로컬 재료, 문장별 워치독으로 TTS가 죽어도
  전진). 반복·뜻 읽기 토글, 목록 탭 건너뛰기. 기능 그리드에서 진입.
- **🧠 리콜 러시**: 밀린 패턴 리콜이 2개 이상이면 성장 화면에 카드가 뜨고,
  최대 10개를 몰아서 상황 리콜 → SRS 일괄 갱신.
- **🎯 입 트임 본인 목표**: 지연 데이터가 3주 이상 쌓이면 "지난달(7~35일 전)
  중앙값 -20%"를 목표로 제시, 이번 주 중앙값과 달성 여부 표시. 절대 기준 금지
  원칙 유지.
- **교정 유형 태그**: AI 교정이 error_type(시제/관사/전치사/어순/단어선택)을
  분류(스키마 확장 — 추가 호출 없음), 정규화 후 저장, 약점 카드에 유형 칩.

가드: 45-audio(7항목), 46-iteration2(7항목). 46개 e2e 파일 전부 통과.
남은 제안: 기술 부채 상환(아래 5장) — 특히 lessons.json 동적화.

## 5. 기술 부채 상환 — lessons.json 동적화 완료 (v0.98.0)

- lib/lessonData.ts: 비동기 로더(loadLessons/useLessons/lessonsNow) 신설.
  lessons.json(199KB)은 이제 어떤 화면 청크에도 정적으로 묶이지 않는
  별도 비동기 청크 — 홈 유휴 프리로드가 가장 먼저 데운다.
- 순수 헬퍼(cefrOf·lessonLabel)는 lib/cefr.ts로 이동(구조적 타입, 순환 없음).
  소비처 12곳 전환: 게이트 화면 6(레슨·드릴·회화·쉐도잉·숙제·진도)은
  page.tsx의 <LessonsGate> 아래에서 lessonsNow() 동기 접근, CEFR 전용 4곳은
  lib/cefr 직행, 대화 변형 생성기는 핸들러에서 await loadLessons().
- lessonsNow()는 게이트 밖 호출 시 즉시 throw — 조용한 빈 화면 대신
  개발 중에 잡히게 설계.
- 측정(동일 컨테이너 A/B): 회화 탭 전환 **871ms → 455ms**. 레슨 탭은
  613~673ms로 불변 — 병목이 데이터가 아니라 "첫 탭"의 공통 청크 다운로드 +
  홈 언마운트(~470ms)임이 확정됨(다음 부채 항목).
- 46개 e2e 전부 통과. 데이터·스키마 변경 없음.

## 6. 이터레이션 3 — 완료 (v0.99.0)

앞 장의 제안 4건 전부 구현:
1. **patternStories 비동기화**(lib/storyData.ts + <StoriesGate>) — 홈 First
   Load JS 173→161KB. 세션·리콜 러시·오디오·성장은 게이트 아래에서 동기 접근,
   홈 CTA는 로드 후 채움. lessonData와 같은 패턴의 두 번째 적용.
2. **프리로드 2단계화** — 1단계(유휴 ~1.2s): 레슨 데이터+스토리+레슨 화면,
   2단계(~4s): 나머지 화면. 첫 탭이 무는 다운로드를 더 이른 유휴로 이동.
3. **오디오 모드**: 🐢0.85× 속도 토글(재생 체인에 반영), 최근 정착 패턴
   2개는 대화 전체를 재생목록에 포함(어제 배운 장면 통째 복습).
4. **주간 시험 추이 차트**(진도, 2회부터) + **유형 집중 드릴**(같은 유형
   교정 5건↑이면 "시제 집중 드릴 →" 등 유형별 큐 자동 개방).

가드: 47-iteration3(7항목). 47개 e2e 전부 통과.

## 7. 다음 이터레이션 제안
1. 레슨 탭 첫 클릭(641ms) 잔여 — 홈 언마운트(~470ms)가 지배적. 앱 셸을
   화면 캐시(keep-alive) 구조로 바꾸는 큰 수술이 유일한 해법으로 남음.
2. 시도 로그 1,000건 근접 시 일별 롤업 캐시.
3. globals.css(~8,500줄) 모듈 분리.
