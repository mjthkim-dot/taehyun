# PLAN — Taehyun 앱 고도화: "아는 영어"에서 "나오는 영어"로

> 미션: 사용자의 문제는 지식 부족이 아니라 **자동화(automatization) 부족**이다.
> 앱은 읽는 도구가 아니라 훈련 루프여야 한다.

## 1. 탐색 결과 (2026-08-12, v0.94.0 기준)

### 1-1. 현재 구조

- Next.js 14 App Router PWA(오프라인 동작), 화면 26+개 전부 `dynamic()` 지연 로드
- 저장은 localStorage 단일 계층(`lib/state.ts`의 load/store), 키 30여 개, 용량 관리용 EVICTABLE 목록 존재
- AI는 Groq 프록시(llama-3.3-70b / whisper / Orpheus TTS), 전 표면 aiGuard(한국어 검증+재시도) 적용
- e2e 39파일(약 7분), 성능·접근성·언어 가드까지 회귀 방어 중

### 1-2. 미션 우선순위 대비 현황 (추측 아님 — 코드 확인)

| 미션 항목 | 현황 | 실제 갭 |
|---|---|---|
| P0 SRS 엔진 | **있음** — `va_weak` 박스식 SM-2 근사(0/1/3/7/16/35일), 4단 자가채점, 오늘 큐(`buildTodayQueue`), 드릴 점수 연동 | ① **문장별 학습 이력이 없다** — 시도 기록이 어디에도 안 남아 추이·난이도 분석 불가. ② **패턴 SRS가 없다** — 40개 커리큘럼 패턴은 한 번 정착하면 다시 안 나옴(D+1/D+7 재소환 부재) |
| P0 말하기 루프 | **있음** — TTS 듣기→Whisper/WebSpeech STT→단어별 diff 채점→발음 진단(한국어 화자 축 15종) | **자동화 측정·훈련이 없다** — 한 번 맞히면 끝. "얼마나 빨리 입이 떨어지는가"(발화 개시 지연)를 재지 않고, 반복으로 매끄럽게 만드는 단계가 없다 |
| P1 AI 튜터 | **있음** — 대화(스트리밍), 배경 교정, CAF 분석, 상황 점검, 대화 로그 저장(`va_chat_logs`) | 교정 결과가 **휘발된다** — 자주 틀리는 패턴이 축적되지 않아 맞춤 훈련 추천 불가. 세션에서 배운 패턴이 대화로 이어지는 통로 없음 |
| P1 대시보드 | **부분** — 스트릭·스킬(GSE)·주간 리포트·CAF 추이(`va_sessions`) | 정확도 추이·자동화(지연) 추이·약점 통합 뷰 없음 — **이력 로그가 없어서 만들 수 없었던 것** |
| P2 실전 모드 | **있음** — 세일즈 시나리오 9종, 미팅 준비/회고, 미션 롤플레이, 2분 피치 | 배운 패턴을 실전에서 썼는지 추적 없음. 주간 측정 리추얼 없음 |

### 1-3. 핵심 발견 (설계를 결정지은 사실)

1. **발화 개시 지연을 이미 잴 수 있다** — `lib/stt.ts`의 RMS 루프가 첫 유성 시점(`lastLoud` 최초 설정)을 이미 감지한다. 반환값에 `voiceOnsetMs` 한 필드만 추가하면 "프롬프트를 보고 몇 초 만에 입이 떨어졌나"가 측정된다. 이것이 자동화의 가장 정직한 지표다.
2. **시도(attempt) 단위 기록이 전무하다** — `evaluateSpeech`는 점수를 화면에 쓰고 버린다. 남는 건 발화 수 합계(`va_spoken_log`)와 발음 축 누적(`va_pron`)뿐.
3. 학습 항목 저장소가 4갈래로 분산 — `va_weak`(SRS), `va_phrases`(표현장), `va_maturity_patterns`(1회 정착), `va_ladder_done`. 통합 "복습 엔진"이 이들 위에 없다.

### 1-4. 기술 부채 (이번 범위에서 다루지 않되 기록)

- `globals.css` 단일 파일 ~7,400줄 / lessons.json 199KB 정적 임포트(회화·레슨 탭 전환 ~600-870ms의 근본 원인) / e2e 직렬 실행 7분
- 이유: 이번 미션은 학습 루프이며, 위 부채는 동작을 위협하지 않음. REPORT.md에 다음 이터레이션 후보로 재기록 예정.

---

## 2. 아키텍처 결정 (트레이드오프 검토)

### D1. 학습 이력 = 별도 append-only 로그 (`va_attempt_log`)

- **대안 A**: `va_weak` 스키마에 이력 배열 내장 → 핫 키 비대화 + 마이그레이션 위험(기존 데이터 재작성 필요) → 기각
- **대안 B (채택)**: 시도마다 `{t, en, score, latencyMs, durationMs, src, patternKey?}`를 별도 로그에 append. 상한 1,000건(초과 시 오래된 것부터 절삭), EVICTABLE 등록.
- **근거**: 기존 키를 한 바이트도 건드리지 않으므로 **데이터 유실 위험이 구조적으로 0**. 추이 차트·항목별 난이도·자동화 지표가 전부 이 로그 하나에서 파생된다. 집계는 읽기 시 계산(하루 1,000건 이하 규모에서 충분히 빠름 — 성능 하니스로 검증 예정).

### D2. 패턴 SRS = 신규 `va_pattern_srs` + 1회성 시드 마이그레이션

- 패턴 정착 시 `{key, box:1, due:D+1}` 생성. 기존 SRS 간격표(1/3/7/16/35일) 재사용 — 새 파라미터를 발명하지 않는다.
- **마이그레이션**: 이미 정착된 `va_maturity_patterns` 항목들을 최초 로드 시 `due=내일`로 시드(멱등 — 이미 있으면 건너뜀). 원본 키는 그대로 둔다(성숙도 승급 계산이 계속 읽으므로).
- 복습 방식: 세션 워밍업에 due 패턴의 **실전 리콜**(상황 지문만 보고 원어민 문장 떠올리기)로 등장. 80점↑ = box 상승, 미만 = box 하락. 리콜이 재인(다시 읽기)보다 기억을 강하게 만든다는 것이 근거.

### D3. 자동화 지표 = 발화 개시 지연(voice onset latency)

- `recordAndTranscribe` 반환에 `voiceOnsetMs` 추가(첫 RMS 초과 시점 − 시작 시점). 기존 필드 불변(추가만).
- 점수 줄에 "⚡ 0.8초 만에 시작"처럼 표시하고 로그에 기록. **주의**: 절대값 비교가 아니라 **본인의 추이**만 보여준다(기기·환경 편차가 크므로 절대 기준선은 만들지 않는다 — 판단 근거).
- **유창성 반복(2연속)**: 세션의 "원어민" 말하기 단계는 80점↑ **2회 연속**이어야 통과(건너뛰기는 유지). 같은 문장을 연달아 말할 때 두 번째의 지연·점수가 좋아지는 경험 자체가 자동화 훈련이다.

### D4. 교정 축적 = `va_mistakes` (상한 200)

- 회화 배경 교정에서 `is_correct=false`인 결과의 `{wrong, right, feedback, t}`를 저장. 대시보드에 "자주 틀리는 유형"으로 노출, 원탭으로 드릴 큐 핸드오프(기존 `va_drill_queue` 재사용).

### D5. 전이(transfer) = 세션→회화 핸드오프 + 패턴 사용 감지

- 세션 완주 화면에 "오늘 패턴으로 2분 대화 →" — 기존 미션 핸드오프(`va_mission_talk`) 재사용, 체크리스트에 오늘 패턴 포함.
- 각 패턴에 정적 `stem`(소문자 매칭 문자열, 예: `"i'd like to"`)을 추가하고, 회화에서 사용자 발화에 stem이 나타나면 `va_pattern_use`에 기록 + 짧은 축하 배지. **결정적 문자열 매칭**(AI 판정 아님) — 오탐 없이 테스트 가능해야 하므로.

### D6. 하지 않기로 한 것

- IndexedDB 이전(현 규모에서 불필요한 복잡도), 서버 저장(개인용 로컬 우선 원칙 유지), 절대 기준 자동화 점수(기기 편차), 신규 과금 요소(없음 — Groq 사용 패턴 불변).

---

## 3. 단계별 범위와 완료 기준

> 매 단계 공통 완료 기준: `tsc` 통과 · 프로덕션 빌드 · e2e 전체 통과(신규 가드 포함) · Playwright 스크린샷으로 UI 직접 확인 · 단계별 커밋 · main 머지

### Phase 1 — 복습 엔진 통합 (P0) : 이력 로그 + 패턴 SRS
- `lib/reviewEngine.ts`: `logAttempt()`, `attemptStats()`(일별 집계), `va_pattern_srs` 시드 마이그레이션(멱등), `dueReviews()`(문장 SRS + 패턴 리콜 통합 큐)
- 세션 워밍업이 통합 큐를 사용(문장 최대 2 + due 패턴 리콜 최대 1)
- SpeakingPractice가 채점 시 `logAttempt` 호출(전 화면 공통 — 세션·드릴·사다리 자동 포함)
- **AC**: ① 구버전 데이터(va_weak·va_maturity_patterns만 있는 상태)를 시드한 e2e에서 기존 항목 전부 보존 + 패턴 SRS 자동 시드 ② 세션 워밍업에 due 패턴 리콜 등장 ③ 시도마다 로그 1건 append(상한 동작 포함)

### Phase 2 — 자동화 훈련 루프 (P0) : 지연 측정 + 2연속 반복
- `stt.ts`에 `voiceOnsetMs`(추가 필드), SpeakingPractice 점수 줄에 개시 지연 표시, 로그에 기록
- 세션 "원어민" 단계 2연속 통과 규칙(진행 표시 ●○, 건너뛰기 유지)
- **AC**: ① 모의 오디오 e2e에서 voiceOnsetMs가 결정적으로 기록 ② 2연속 UI가 1회 통과 시 ●○, 2회 시 다음 버튼 활성 ③ 지연 표시가 측정 불가 환경(WebSpeech 폴백)에서는 조용히 생략

### Phase 3 — 전이 루프 (P1) : 세션→회화 + 교정 축적
- 세션 완주 → "오늘 패턴으로 2분 대화" 핸드오프, 패턴 stem 감지 → `va_pattern_use` 기록 + 배지
- 배경 교정 실패분 `va_mistakes` 축적, 성장/진도 화면에 "자주 틀리는 패턴 → 드릴로" 추천
- **AC**: ① 핸드오프로 회화 진입 시 오늘 패턴이 시나리오에 반영 ② stem 포함 발화 시 기록+배지 e2e ③ 교정 2건 축적 후 추천 카드 노출·드릴 핸드오프 동작

### Phase 4 — 학습 대시보드 (P1)
- 진도 화면 확장: 정확도 추이(14일) · 발화 개시 지연 추이 · 약점 통합(발음 축 + 교정 유형) · 실전 사용 카운트
- **UI 시안 2개**(카드 나열형 vs 요약 히어로+상세형)를 스크린샷으로 비교 제시 후 선택 반영
- **AC**: 시드 데이터로 차트 렌더 e2e, 데이터 0건일 때 빈 상태 안내

### Phase 5 — 실전 리추얼 (P2)
- 주간 말하기 시험: 일요일(또는 7일 경과) 홈 CTA → 이번 주 패턴으로 1분 스피치 → 지표 기록·추이
- **AC**: 조건부 CTA 노출, 완료 기록, 추이 반영

### 마무리 — REPORT.md
- 변경 요약, 지표 전/후, 남은 개선점(기술 부채 포함), 다음 이터레이션 제안

## 4. 리스크와 방어

| 리스크 | 방어 |
|---|---|
| 기존 학습 데이터 유실 | 모든 변경이 **추가형**(기존 키 스키마 불변). 유일한 쓰기 마이그레이션(패턴 SRS 시드)은 멱등 + 원본 비파괴. 구버전 데이터 시드 e2e로 검증 |
| localStorage 용량 | 로그 상한 1,000건·교정 200건, EVICTABLE 등록. 성능 하니스로 저장소 크기 확인 |
| 자동화 지표 오해 | 절대 비교 금지 — 본인 추이만. 측정 불가 시 UI에서 생략 |
| 세션 마찰 증가(2연속) | 건너뛰기 항상 유지. 워밍업 총량 상한 3(기존과 동일) |
| e2e 시간 증가 | 신규 파일 2~3개 예상(40~42). 직렬 7분→8분대 허용, 병렬화는 부채 목록에 |
